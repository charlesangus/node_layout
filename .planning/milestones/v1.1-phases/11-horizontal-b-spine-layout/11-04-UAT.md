---
status: diagnosed
phase: 11-horizontal-b-spine-layout
source: 11-04-PLAN.md (gap closure re-verification)
started: 2026-03-15T00:00:00Z
updated: 2026-03-15T00:03:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Output Dot after Layout Selected Horizontal
expected: Create a linear chain A -> B -> C -> D. Select all 4. Run "Layout Selected Horizontal". An output Dot appears below D (the rightmost node).
result: issue
reported: "an extra dot was created at the top input even though there already was one"
severity: major

### 2. Downstream node triggers horizontal replay
expected: Using the same chain from Test 1 (mode='horizontal' stored on all nodes), scramble positions. Select D (the rightmost node). Run "Layout Upstream". The chain lays out horizontally — not vertically. Then select a node even further downstream (E with D as input). Run "Layout Upstream". Horizontal replay still fires via the ancestor walk.
result: issue
reported: "Still fires, but is not placed correctly. the whole horizontal subtree must go to the right of the downstream node, same as a vertical group. instead it is directly above."
severity: major

### 3. Place Only stores mode — subsequent layout replays horizontal
expected: Create a new chain with side subtrees. Select spine nodes. Run "Layout Selected Horizontal (Place Only)". Scramble positions. Select any spine node. Run "Layout Upstream". Horizontal replay fires — the layout is horizontal, not vertical.
result: issue
reported: "same issues as the non-place version — duplicate dot at top input, and horizontal subtree placed above instead of to the right of the downstream node"
severity: major

## Summary

total: 3
passed: 0
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "Running Layout Selected Horizontal on a chain that already has an output Dot below the root should reuse that Dot, not create a duplicate"
  status: failed
  reason: "User reported: an extra dot was created at the top input even though there already was one"
  severity: major
  test: 1
  root_cause: "_place_output_dot_for_horizontal_root uses Python `is` for node identity at lines 449 and 453, but Nuke's API may return fresh proxy wrapper objects — the rest of the codebase uses id() for this reason. The `is` check fails to find the existing Dot, so existing_dot is never set, and a new Dot is created instead of reusing."
  artifacts:
    - path: "node_layout.py:449"
      issue: "if node.input(0) is root — should be id(node.input(0)) == id(root)"
    - path: "node_layout.py:453"
      issue: "if node.input(slot) is root — should be id(node.input(slot)) == id(root)"
  missing:
    - "Replace `is` checks with `id()` comparisons at lines 449 and 453, adding `is not None` guards"
  debug_session: ".planning/debug/duplicate-output-dot-on-rerun.md"
- truth: "When a horizontal chain is replayed from a downstream node, the horizontal subtree is placed to the RIGHT of that downstream node (not above it), mirroring how a vertical group is positioned relative to its downstream consumer"
  status: failed
  reason: "User reported: Still fires, but is not placed correctly. the whole horizontal subtree must go to the right of the downstream node, same as a vertical group. instead it is directly above."
  severity: major
  test: 2
  root_cause: "In layout_upstream, when the ancestor walk rebinds root from E to the horizontal ancestor D, spine_x/spine_y are computed with vertical placement semantics (D placed above E). Should use horizontal placement: spine_x = E.xpos() + E.screenWidth() + horizontal_gap, spine_y = E.ypos() — placing D to the right of E at the same Y. Same fix needed in layout_selected, which uses root's stale coordinates entirely rather than anchoring to the originally selected node."
  artifacts:
    - path: "node_layout.py:1313-1321"
      issue: "layout_upstream: spine_x/spine_y use vertical formula (above consumer) instead of horizontal (right of consumer)"
    - path: "node_layout.py:1476"
      issue: "layout_selected: always uses root.xpos()/root.ypos() (stale) with no downstream-consumer anchoring after ancestor walk"
  missing:
    - "layout_upstream: replace vertical spine_x/spine_y formula with: spine_x = original_selected_root.xpos() + original_selected_root.screenWidth() + horizontal_gap; spine_y = original_selected_root.ypos()"
    - "layout_selected: add original_selected_root save-and-anchor pattern matching layout_upstream fix"
  debug_session: ".planning/debug/horiz04-downstream-replay-wrong-position.md"
- truth: "After Place Only, running Layout Upstream replays the horizontal subtree to the RIGHT of the downstream node (not above), and no duplicate dot is created"
  status: failed
  reason: "User reported: same issues as the non-place version — duplicate dot at top input, and horizontal subtree placed above instead of to the right of the downstream node"
  severity: major
  test: 3
  root_cause: "Same two root causes as tests 1 and 2 — the `is` identity check failure in _place_output_dot_for_horizontal_root and the vertical placement formula in layout_upstream/layout_selected. Place Only now correctly writes mode='horizontal', so replay fires; but both downstream fixes still apply."
  artifacts:
    - path: "node_layout.py:449,453"
      issue: "id() vs is — same as test 1"
    - path: "node_layout.py:1313-1321"
      issue: "vertical placement formula — same as test 2"
  missing:
    - "Same fixes as tests 1 and 2"
  debug_session: ""
