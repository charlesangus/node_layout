---
phase: 11-horizontal-b-spine-layout
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_horizontal_layout.py
autonomous: true
requirements:
  - HORIZ-01
  - HORIZ-02
  - HORIZ-03

must_haves:
  truths:
    - "tests/test_horizontal_layout.py exists with 6 test classes and 10 test methods, all failing RED"
    - "TestHorizontalSpine verifies root is placed at spine_x and each successive input[0] ancestor steps left"
    - "TestOutputDot verifies Dot is created below root when consumer exists and is reused (not recreated) on replay"
    - "TestMaskKink verifies downstream spine nodes drop in Y by the mask subtree height"
    - "TestSideInputPlacement verifies side inputs are placed above (lower Y) their spine node"
    - "TestHorizontalAST verifies layout_upstream_horizontal and layout_selected_horizontal exist as functions in node_layout.py"
    - "TestModeReplay verifies layout_upstream() body contains both 'horizontal' string and 'place_subtree_horizontal' reference"
  artifacts:
    - path: "tests/test_horizontal_layout.py"
      provides: "RED test scaffold — behavioral contracts for all HORIZ-01/02/03 behaviors"
      min_lines: 200
  key_links:
    - from: "tests/test_horizontal_layout.py"
      to: "node_layout.py"
      via: "spec_from_file_location with alias 'node_layout_horizontal'"
      pattern: "node_layout_horizontal"
---

<objective>
Create the RED test scaffold for Phase 11 horizontal B-spine layout. All 10 tests must fail RED on creation (implementation does not yet exist) and define the behavioral contracts that Plans 02 and 03 will satisfy.

Purpose: Establish acceptance criteria before implementation. Tests drive the API signatures for place_subtree_horizontal(), _find_or_create_output_dot(), and mode dispatch in layout_upstream().
Output: tests/test_horizontal_layout.py with 6 classes, 10 failing tests.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-horizontal-b-spine-layout/11-CONTEXT.md
@.planning/phases/11-horizontal-b-spine-layout/11-RESEARCH.md
</context>

<feature>
  <name>Horizontal B-Spine Layout — RED Test Scaffold</name>
  <files>tests/test_horizontal_layout.py</files>
  <behavior>
    TestHorizontalSpine:
      - test_root_placed_at_spine_x: root node xpos == spine_x after place_subtree_horizontal call
      - test_two_node_spine_step_left: second spine node xpos == root.xpos() - step_x - ancestor.screenWidth()

    TestOutputDot:
      - test_output_dot_created_below_root: _find_or_create_output_dot creates a Dot with xpos centered on root
      - test_output_dot_reused_on_replay: calling _find_or_create_output_dot twice returns the same Dot object (assertIs)

    TestMaskKink:
      - test_mask_kink_drops_downstream_spine: spine node downstream of masked ancestor has ypos == spine_y + mask_height + margin

    TestSideInputPlacement:
      - test_side_input_placed_above_spine_node: side input ypos < spine node ypos (lower Y = higher on screen)

    TestHorizontalAST:
      - test_layout_upstream_horizontal_exists: ast.parse(node_layout source) contains FunctionDef named layout_upstream_horizontal
      - test_layout_selected_horizontal_exists: ast.parse(node_layout source) contains FunctionDef named layout_selected_horizontal
      - test_compute_dims_memo_key_includes_node_h_scale: 'node_h_scale' appears within 80 chars of 'memo[' in compute_dims source

    TestModeReplay:
      - test_layout_upstream_dispatches_to_horizontal: layout_upstream() function source contains both 'horizontal' and 'place_subtree_horizontal'

    Stub requirements:
      - _StubNode with xpos/ypos/screenWidth/screenHeight/setXpos/setYpos/inputs/input/setInput/inputLabel/knob
      - _StubKnob with value/setValue
      - _StubDotNode subclass of _StubNode with optional node_layout_output_dot knob
      - nuke stub module with: nodes.Dot returning _StubDotNode, Tab_Knob, Int_Knob, selectedNodes, allNodes, lastHitGroup returning None
      - Load node_layout.py via spec_from_file_location with unique alias 'node_layout_horizontal' to avoid sys.modules collisions
  </behavior>
  <implementation>
    This is a RED-only plan. Tests must fail on creation because place_subtree_horizontal, _find_or_create_output_dot, layout_upstream_horizontal, layout_selected_horizontal, and the horizontal dispatch in layout_upstream do not yet exist. Do NOT implement any production code. Verify RED with: python3 -m unittest tests/test_horizontal_layout.py -q and confirm all 10 tests fail.
  </implementation>
</feature>

<verification>
python3 -m unittest tests/test_horizontal_layout.py -q

Expected: 10 failures (all RED). Zero passes.
Full suite unchanged: python3 -m unittest discover tests/ -q passes all pre-existing tests.
</verification>

<success_criteria>
tests/test_horizontal_layout.py exists with 6 test classes and 10 test methods. All 10 fail RED. Pre-existing 237 tests still pass. 4 pre-existing test_scale_nodes_axis errors remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/11-horizontal-b-spine-layout/11-01-SUMMARY.md`
</output>
