---
phase: 11.2-fix-horizontal-layout-bbox
verified: 2026-03-17T06:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Run layout_upstream on a real Nuke script with a horizontal chain that has a wide side input, then inspect clearance visually"
    expected: "Chain sits visibly clear of consumer with a noticeable gap (horizontal_subtree_gap = 250px)"
    why_human: "Node position arithmetic verified programmatically; visual layout quality and pixel-accurate rendering in Nuke DAG cannot be confirmed without the Nuke host"
  - test: "Run layout_upstream on a Nuke script where the consumer has both a horizontal chain (slot 0) and a B-chain (slot 1), then confirm no visual overlap"
    expected: "B-chain node sits clearly to the left of the horizontal chain with a gap between them"
    why_human: "Phase 2 anchor clamping verified arithmetically in tests; visual inspection in the actual DAG is the final acceptance gate"
---

# Phase 11.2: Fix Horizontal Layout Bbox Verification Report

**Phase Goal:** Layout Upstream with a horizontal chain produces no overlaps: the chain clears the consumer by horizontal_subtree_gap, and the Phase 2 vertical subtrees do not land inside the horizontal chain's bounding box.
**Verified:** 2026-03-17T06:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `min_x(chain_nodes) >= consumer.xpos() + consumer.screenWidth() + horizontal_subtree_gap` after `layout_upstream()` | VERIFIED | `TestBugAChainClearsConsumer.test_chain_clears_consumer_by_gap` passes (OK) |
| 2 | `max_x(b_chain_nodes) < min_x(chain_nodes) - horizontal_subtree_gap` after `layout_upstream()` | VERIFIED | `TestBugBPhase2NoCrossChain.test_phase2_does_not_overlap_horizontal_chain` passes (OK) |
| 3 | `layout_selected` replay branch has Fix A and Fix B applied (parity with `layout_upstream`) | VERIFIED | Identical blocks found at `node_layout.py` lines 1735–1825; structurally identical to `layout_upstream` blocks at lines 1417–1513 |
| 4 | All 274 previously-passing tests continue to pass (zero regressions) | VERIFIED | Full suite: 276 tests run, 0 unexpected failures; 4 pre-existing Undo errors only (test_scale_nodes_axis.py, pre-dates this phase) |
| 5 | RED TDD scaffold exists: `TestBugAChainClearsConsumer.test_chain_clears_consumer_by_gap` uses full `layout_upstream()` call stack | VERIFIED | Test class at line 1871 of `tests/test_horizontal_layout.py`; calls `nl.layout_upstream()` with no mocked internals |
| 6 | RED TDD scaffold exists: `TestBugBPhase2NoCrossChain.test_phase2_does_not_overlap_horizontal_chain` uses full `layout_upstream()` call stack | VERIFIED | Test class at line 1979 of `tests/test_horizontal_layout.py`; calls `nl.layout_upstream()` with no mocked internals |
| 7 | Fix A shift loop appears after `place_subtree_horizontal` and before `_place_output_dot_for_horizontal_root` in both functions | VERIFIED | `node_layout.py` lines 1417–1444 (`layout_upstream`) and 1735–1762 (`layout_selected`); `_place_output_dot_for_horizontal_root` follows at lines 1446 and 1764 respectively |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_horizontal_layout.py` | RED TDD scaffold: `TestBugAChainClearsConsumer`, `TestBugBPhase2NoCrossChain` | VERIFIED | Both classes present at lines 1871 and 1979; setUp/tearDown mirrors `TestLayoutUpstreamEndToEnd`; tests call full `layout_upstream()` stack |
| `node_layout.py` | Fix A: place-then-measure-then-shift clearance; Fix B: Phase 2 anchor clamped to chain bbox | VERIFIED | Fix A contains `_required_left = original_selected_root.xpos() + original_selected_root.screenWidth() + _horizontal_gap` (line 1426); `_clearance_deficit` shift loop (lines 1431–1434); Fix B contains `_chain_left_for_phase2` bbox capture (line 1456) and `phase2_anchor_x` clamping (lines 1496–1504) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `TestBugAChainClearsConsumer.test_chain_clears_consumer_by_gap` | `nl.layout_upstream()` | Direct call at line 1953 | WIRED | Test calls `nl.layout_upstream()` then asserts `actual_left >= required_left` |
| `TestBugBPhase2NoCrossChain.test_phase2_does_not_overlap_horizontal_chain` | `nl.layout_upstream()` | Direct call at line 2051 | WIRED | Test calls `nl.layout_upstream()` then asserts `b_max_x < chain_min_x - gap` |
| `layout_upstream` if-branch — after `place_subtree_horizontal` | `collect_subtree_nodes(root)` + `compute_node_bounding_box` | Fix A shift loop (lines 1422–1434) | WIRED | `_chain_nodes_fix_a = collect_subtree_nodes(root)`, minimum x computed, deficit shift applied |
| `layout_upstream` Phase 2 block | `place_subtree(original_selected_root, phase2_anchor_x, ...)` | Fix B clamp (lines 1496–1504) | WIRED | `phase2_anchor_x` computed then passed to `place_subtree` at line 1505 |
| `layout_selected` replay block — after `place_subtree_horizontal` | same Fix A/B pattern | Structurally identical blocks at lines 1735–1825 | WIRED | Identical variable names and logic to `layout_upstream` blocks |

---

### Requirements Coverage

REQUIREMENTS.md does not define BUG-A, BUG-B, or REGRESSION as formal requirement IDs in its v1.1 requirements table — these are phase-internal defect identifiers declared in ROADMAP.md (line 165) and CONTEXT.md (line 381–382). REQUIREMENTS.md maps this phase to no formal requirement IDs; Phase 11 (HORIZ-01, HORIZ-02, HORIZ-03) covers horizontal layout correctness and is already marked complete. Phase 11.2 is a bug-fix insertion that sharpens the invariants established by Phase 11 without introducing new named requirements.

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| BUG-A | ROADMAP.md line 165; PLAN 01 + PLAN 02 frontmatter | `min_x(chain_nodes) >= consumer.right + horizontal_subtree_gap` after layout | SATISFIED | Test `test_chain_clears_consumer_by_gap` passes; Fix A shift loop in `node_layout.py` line 1417 |
| BUG-B | ROADMAP.md line 165; PLAN 01 + PLAN 02 frontmatter | `max_x(b_chain_nodes) < min_x(chain_nodes) - horizontal_subtree_gap` after layout | SATISFIED | Test `test_phase2_does_not_overlap_horizontal_chain` passes; Fix B clamp in `node_layout.py` line 1489 |
| REGRESSION | ROADMAP.md line 165; PLAN 02 frontmatter | All 274 previously-passing tests continue to pass | SATISFIED | 276 tests run, 4 pre-existing Undo errors (unchanged since Phase 10), 0 new failures |

**Orphaned requirements check:** REQUIREMENTS.md traceability table lists no entries for Phase 11.2 — consistent with this being a bug-fix phase using defect IDs rather than formal REQUIREMENTS.md IDs. No orphaned requirements found.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | — | — | No anti-patterns found |

Scan of `node_layout.py` and `tests/test_horizontal_layout.py` (the two files modified in this phase):
- Zero `TODO`, `FIXME`, `XXX`, or `HACK` comments in `node_layout.py`
- Fix A and Fix B implementations are substantive: no `return null`, empty handlers, or console-log-only stubs
- Test assertions reference actual computed values from `layout_upstream()`, not hardcoded stubs

---

### Human Verification Required

#### 1. BUG-A clearance: visual inspection in Nuke DAG

**Test:** In a real Nuke session, create a horizontal chain (two nodes set to horizontal mode) feeding into a consumer, then run Layout Upstream. Inspect the DAG.
**Expected:** The leftmost node of the horizontal chain sits at least `horizontal_subtree_gap` (250 px) to the right of the consumer's right edge. No visual crowding or overlap.
**Why human:** Node positions are verified arithmetically by unit tests. Pixel-accurate rendering and visual spacing quality in the actual Nuke DAG require a running Nuke host.

#### 2. BUG-B Phase 2 separation: visual inspection in Nuke DAG

**Test:** In a real Nuke session, create a consumer node with two inputs: slot 0 is a horizontal chain, slot 1 is a vertical B-chain node. Run Layout Upstream. Inspect the DAG.
**Expected:** The B-chain node appears clearly to the left of the horizontal chain, with a visible gap between them. No overlap between the vertical and horizontal subtrees.
**Why human:** The anchor clamping formula is verified in unit tests. Confirming the rendered spatial arrangement in the Nuke DAG requires visual inspection.

---

### Gaps Summary

No gaps. All must-haves are verified:

- Both acceptance tests (`test_chain_clears_consumer_by_gap` and `test_phase2_does_not_overlap_horizontal_chain`) pass with full `layout_upstream()` call stacks.
- Fix A (place-then-measure-then-shift) is substantively implemented and wired in both `layout_upstream` (line 1417) and `layout_selected` (line 1735).
- Fix B (Phase 2 anchor clamping) is substantively implemented and wired in both `layout_upstream` (line 1489) and `layout_selected` (line 1801).
- 276 tests total, 0 unexpected failures. The 4 errors are all pre-existing `nuke.Undo` attribute errors in `test_scale_nodes_axis.py` that predate this phase.
- BUG-A, BUG-B, and REGRESSION requirements are all satisfied.
- No orphaned requirements in REQUIREMENTS.md (phase 11.2 uses defect IDs, not formal requirement IDs).
- Two human verification items identified for visual/DAG-render confirmation in a running Nuke host.

---

_Verified: 2026-03-17T06:00:00Z_
_Verifier: Claude (gsd-verifier)_
