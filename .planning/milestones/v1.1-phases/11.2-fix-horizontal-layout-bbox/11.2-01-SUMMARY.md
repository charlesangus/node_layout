---
phase: 11.2-fix-horizontal-layout-bbox
plan: 01
subsystem: testing
tags: [python, unittest, tdd, horizontal-layout, bbox, clearance]

# Dependency graph
requires:
  - phase: 11.2-fix-horizontal-layout-bbox
    provides: CONTEXT.md and RESEARCH.md identifying BUG-A spine_x mismatch and BUG-B Phase 2 overlap
provides:
  - RED TDD scaffold: TestBugAChainClearsConsumer.test_chain_clears_consumer_by_gap
  - RED TDD scaffold: TestBugBPhase2NoCrossChain.test_phase2_does_not_overlap_horizontal_chain
affects: [11.2-02-PLAN (Fix A + Fix B implementation)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wide-side-input topology for triggering effective_widths mismatch: n(num_inputs=2, slot 1 = wide a) triggers effective_widths[n] > n.screenWidth()"
    - "Use _node_layout_prefs_module.DEFAULTS[key] (not nl._PREFS_DEFAULTS) for pref access in test assertions"

key-files:
  created: []
  modified:
    - tests/test_horizontal_layout.py

key-decisions:
  - "BUG-A test needs wide side input on spine node (a, width=500) to make effective_widths[n] >> n.screenWidth() — simple spine with no side inputs satisfies the clearance invariant even without Fix A"
  - "Use _node_layout_prefs_module.DEFAULTS not nl._PREFS_DEFAULTS — nl is loaded as node_layout_horizontal alias; DEFAULTS lives in node_layout_prefs module"

patterns-established:
  - "TDD RED topology must actually exercise the bug path — simple topologies may accidentally satisfy the acceptance criterion; use wide side-input nodes to force effective_widths mismatch"

requirements-completed: [BUG-A, BUG-B]

# Metrics
duration: 12min
completed: 2026-03-16
---

# Phase 11.2 Plan 01: RED TDD Scaffold — BUG-A and BUG-B Summary

**Two RED end-to-end tests encoding BUG-A (chain clearance mismatch via wide side-input topology) and BUG-B (Phase 2 B-chain overlaps horizontal chain bbox) added to test_horizontal_layout.py**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-16T12:00:00Z
- **Completed:** 2026-03-16T12:12:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Added `TestBugAChainClearsConsumer.test_chain_clears_consumer_by_gap` — fails RED with AssertionError: 710 not >= 1130
- Added `TestBugBPhase2NoCrossChain.test_phase2_does_not_overlap_horizontal_chain` — fails RED with AssertionError: 1210 not < 880
- Full suite at 276 tests (274 + 2 new), 2 FAILs, 4 pre-existing Undo errors, zero regressions

## Task Commits

1. **Tasks 1 + 2: RED scaffold for BUG-A and BUG-B** - `ea947bf` (test)

**Plan metadata:** (pending final commit)

## Files Created/Modified

- `tests/test_horizontal_layout.py` — added 203 lines: two new test classes with setUp/tearDown, two RED test methods

## Decisions Made

- BUG-A test topology required a wide side-input node (`a`, width=500) on spine node `n` to trigger the `effective_widths` mismatch. A simple chain (n → m2 → m1) with no side inputs satisfies the clearance constraint even before Fix A, so the test would pass GREEN prematurely and not serve as a RED guard.
- Pref access via `_node_layout_prefs_module.DEFAULTS["horizontal_subtree_gap"]` rather than `nl._PREFS_DEFAULTS["horizontal_subtree_gap"]` — the plan's interface spec used `nl._PREFS_DEFAULTS` which does not exist; the module is loaded as alias `node_layout_horizontal` and has no `_PREFS_DEFAULTS` attribute.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Plan's interface spec used non-existent nl._PREFS_DEFAULTS attribute**
- **Found during:** Task 1 (TestBugAChainClearsConsumer)
- **Issue:** Plan specified `nl._PREFS_DEFAULTS["horizontal_subtree_gap"]` but `nl` is loaded with alias `"node_layout_horizontal"` and has no `_PREFS_DEFAULTS` attribute; this caused AttributeError (ERROR not FAIL)
- **Fix:** Replaced with `_node_layout_prefs_module.DEFAULTS["horizontal_subtree_gap"]` matching the existing test file pattern
- **Files modified:** tests/test_horizontal_layout.py
- **Verification:** Test produces AssertionError (FAIL) not AttributeError (ERROR)
- **Committed in:** ea947bf

**2. [Rule 1 - Bug] Plan's BUG-A topology (simple n → m2 → m1) did not trigger the clearance bug**
- **Found during:** Task 1 (TestBugAChainClearsConsumer)
- **Issue:** Simple topology with no side inputs accidentally satisfied `actual_left >= required_left` — the test would pass GREEN before Fix A, defeating its purpose as a RED guard
- **Fix:** Added wide side-input node `a` (width=500) on spine node `n` (input slot 1), giving `effective_widths[n]=290 >> n.screenWidth()=80`; this forces the advance formula to push `n` and `a` far left of `required_left`
- **Files modified:** tests/test_horizontal_layout.py
- **Verification:** Test fails RED: AssertionError 710 not >= 1130
- **Committed in:** ea947bf

---

**Total deviations:** 2 auto-fixed (both Rule 1 - Bug)
**Impact on plan:** Both fixes necessary for test correctness — without them, tests would be false-GREEN guards that don't catch the bugs they're meant to detect. No scope creep.

## Issues Encountered

None beyond the two auto-fixed attribute access and topology bugs.

## Next Phase Readiness

- RED scaffold complete; both failing tests are acceptance criteria for Fix A and Fix B
- Plan 11.2-02 should implement Fix A (place-then-measure-then-shift) and Fix B (Phase 2 anchor clamping) in node_layout.py
- Running `python3 -m unittest tests.test_horizontal_layout.TestBugAChainClearsConsumer tests.test_horizontal_layout.TestBugBPhase2NoCrossChain -v` should turn GREEN after 11.2-02

---
*Phase: 11.2-fix-horizontal-layout-bbox*
*Completed: 2026-03-16*
