---
phase: 16-layout-integration
task: UAT
total_tasks: null
status: in_progress
last_updated: 2026-03-21T09:48:22.710Z
---

<current_state>
Phase 16 plans all completed. Three sessions of iterative freeze block layout fixes.
Session 3 addressed two user-reported issues: horizontal freeze block left margin and
vertical spurious push-right. All code changes uncommitted, awaiting live Nuke UAT.
</current_state>

<completed_work>

- Plans 16-01 through 16-04, spine_set fix: Done (prior sessions, committed)
- Fix `import util` missing from menu.py: Done (uncommitted)
- Refactored compute_dims to 3-tuple (W, H, root_x_offset): Done (uncommitted)
- **Session 3 fixes (this session):**
  - `place_subtree_horizontal` now receives `freeze_block_sizes` param, passes to all `compute_dims` calls
  - Side subtree effective_widths/left_extents use `side_root_x_offset` for correct left/right extent
  - Spine nodes that ARE freeze block roots expand `effective_widths[i]`/`left_extents[i]` to cover full block bbox
  - `_layout_selected_horizontal_impl` now constructs `freeze_block_sizes` dict (was missing → NameError)
  - `layout_selected` computes `bbox_before` BEFORE filtering out non-root freeze members (was causing spurious `grew_right`)
  - `push_nodes_to_make_room` has Y-overlap guard on push-right condition (defense-in-depth)
  - All 335 tests pass

</completed_work>

<remaining_work>

- UAT in live Nuke — THREE test scenarios:
  1. Horizontal layout with freeze block → verify left margin, no overlap with upstream subtree
  2. Vertical layout with freeze block → verify downstream subtrees NOT pushed right
  3. Normal (non-freeze) layout → verify no regression
- If UAT passes: commit menu.py, node_layout.py, and 3 test files
- Update VALIDATION.md and STATE.md
- Push to remote (33+ commits ahead of origin) to close v1.3 milestone

</remaining_work>

<decisions_made>

- `compute_dims` returns `(W, H, root_x_offset)` — 3-tuple. Normal nodes: offset=0. Freeze blocks: offset=left_overhang.
- `place_subtree` reads `root_x_offset` from `child_dims[i][2]` — no freeze_block_sizes param needed
- Centering uses ROOT NODE width, not allocation band width
- Spine nodes that are freeze block roots: `effective_widths[i]` = max(screenWidth, right_ext), `left_extents[i]` = max(0, left_ovh)
- `bbox_before` must include all nodes that `bbox_after` will include (same set) to prevent spurious growth detection
- Push-right only when node vertically overlaps the subtree footprint

</decisions_made>

<blockers>
- Awaiting live Nuke UAT — must test freeze blocks (horizontal AND vertical) and normal layout
- User reported horizontal margin still wrong and vertical push still happening after first fix attempt; second attempt addresses root causes (spine-node-IS-block-root and bbox_before mismatch)
</blockers>

<context>
Three sessions debugging freeze block layout integration. Core architecture: compute_dims
(bottom-up) returns (W, H, root_x_offset), place_subtree (top-down) uses it.

Session 1: 3-tuple refactor. Session 2: user reported horizontal left margin + vertical
push-right still broken. First attempt fixed side-subtree compute_dims but missed that
spine nodes themselves can BE freeze block roots (effective_widths only had screenWidth).
Also missed _layout_selected_horizontal_impl entirely lacked freeze_block_sizes (NameError).
Second attempt: (a) spine effective_widths pre-pass for freeze block roots, (b) bbox_before
moved before filter in layout_selected, (c) Y-overlap guard in push_nodes_to_make_room.

User confirmed approach is right and close to working. Both issues have clear root causes
now addressed. Needs UAT confirmation.
</context>

<next_action>
Start with: Ask user to run live Nuke UAT — test horizontal freeze block margin,
vertical push-right, and normal non-freeze layout. If all pass, commit and push.
</next_action>
