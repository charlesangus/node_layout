---
phase: 16-layout-integration
task: post-gap-closure UAT
total_tasks: ongoing
status: in_progress
last_updated: 2026-03-20T05:02:50.606Z
---

<current_state>
Phase 16 (Layout Integration) gap closure plans executed and verified (9/9 must-haves). Two additional bugs
surfaced during live Nuke UAT that weren't caught by stub tests:

1. **Horizontal layout freeze bug** — `_layout_selected_horizontal_impl` had zero freeze awareness. Added
   freeze preprocessing, offset restoration, and corrected state write-back. Two iterations required:
   - First fix: excluded non-root members from spine_set → BROKEN (breaks spine walk continuity when
     non-root members sit in the input(0) chain between other spine nodes)
   - Second fix (current, committed as 15d84a5): keep all members in spine_set so walk traverses through
     them; offset restoration moves them to relative positions after placement

2. **Vertical mode "broken again"** — user reported vertical also broken after the first (bad) horizontal
   fix. Root cause was the spine_set exclusion disconnecting the spine walk. The second fix (15d84a5)
   should have resolved both. UNCONFIRMED — session paused before user tested the second fix.
</current_state>

<completed_work>

- Plan 16-01: Freeze group preprocessing (_detect_freeze_groups, _find_freeze_block_root, auto-join) — Done
- Plan 16-02: Rigid block positioning and freeze-aware push-away — Done
- Plan 16-03 (gap closure): Added missing `import make_room` to menu.py — Done
- Plan 16-04 (gap closure): Fixed 3 freeze integration bugs:
  - Type mismatch `node_filter -= freeze_excluded_ids` in layout_selected (no-op; fixed with set comprehension)
  - Second pass for non-frozen upstream nodes blocked by excluded freeze members
  - BFS horizontal root search skips frozen candidates in both entry points
- Horizontal layout freeze fix (commit 8ccc5a2, corrected in 15d84a5): added freeze preprocessing to
  `_layout_selected_horizontal_impl` with correct spine_set and offset restoration
- VERIFICATION.md: 9/9 must-haves passed
- UAT: 5/5 original gaps marked resolved

</completed_work>

<remaining_work>

- Re-test horizontal layout with frozen nodes in live Nuke (user was about to test when session paused)
- Re-test vertical layout with frozen nodes (user reported regression; second fix should address)
- If still broken: deeper investigation of _layout_selected_horizontal_impl — specifically whether
  `_detect_freeze_groups` auto-join is incorrectly modifying node state during horizontal layout
- Once UAT passes: ask user about push to remote

</remaining_work>

<decisions_made>

- Freeze block should MOVE during layout (not stay anchored) — user explicitly confirmed blocks are
  supposed to translate as a unit; only relative intra-block positions must be preserved
- Type mismatch root cause for Gap 2/3: `node_filter -= freeze_excluded_ids` is a no-op because
  set[Node] - set[int] is always empty; correct pattern is set comprehension with id() comparison
- Horizontal layout spine_set must include ALL freeze members (including non-root) so spine walk
  can traverse through them; offset restoration handles positioning after placement
- Non-root freeze members should NOT get mode='horizontal' written during horizontal layout state write-back
  (they don't participate in the spine; their mode is irrelevant for replay)

</decisions_made>

<blockers>

- Live Nuke UAT required for final confirmation — stub tests can't catch all real-world Nuke behavior
- UNCONFIRMED: whether the second horizontal fix (15d84a5) actually resolves both reported issues

</blockers>

<context>
Phase 16 is the final phase of the v1.3 Freeze Layout milestone. All planned work is done and
verified in CI (335/335 tests). The remaining work is live Nuke validation only.

The freeze integration is architecturally sound — the preprocessing pipeline (_detect_freeze_groups,
_expand_scope_for_freeze_groups, _find_freeze_block_root) is shared across layout_upstream,
layout_selected, and now _layout_selected_horizontal_impl. The core pattern is:
1. Expand scope to include full freeze groups
2. Detect freeze groups, build freeze_excluded_ids
3. Capture relative offsets before layout
4. Run layout (with all members visible for traversal)
5. Restore offsets after layout so block moves as unit
6. Skip non-root members in state write-back

Key file: node_layout.py, function `_layout_selected_horizontal_impl` (line ~2749).
The function is short (~80 lines in the try block).
</context>

<next_action>
Ask user to re-test horizontal layout and vertical layout with frozen nodes in live Nuke,
starting from a fresh Nuke session to ensure no stale state from previous broken runs.
If both pass, ask about push to remote.
If still broken, read _layout_selected_horizontal_impl (lines 2749-2875) and trace through
a concrete scenario to identify the remaining issue.
</next_action>
