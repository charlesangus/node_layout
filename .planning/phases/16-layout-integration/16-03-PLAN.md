---
phase: 16-layout-integration
plan: "03"
type: execute
wave: 0
depends_on: []
files_modified: [menu.py]
autonomous: true
gap_closure: true
requirements: [FRZE-07]

must_haves:
  truths:
    - "Make Room commands execute without NameError in Nuke"
  artifacts:
    - path: "menu.py"
      provides: "import make_room statement"
      contains: "import make_room"
  key_links:
    - from: "menu.py"
      to: "make_room module"
      via: "import statement"
      pattern: "import make_room"
---

<objective>
Fix the missing `import make_room` in menu.py that causes a NameError when any Make Room command is invoked.

Purpose: Unblock all six Make Room / Expand / Push-Away menu commands — currently every invocation crashes with `NameError: name "make_room" is not defined`.
Output: menu.py with the missing import added.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-layout-integration/16-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing make_room import to menu.py</name>
  <files>menu.py</files>
  <read_first>menu.py</read_first>
  <action>
Add `import make_room` after the existing `import node_layout_prefs_dialog` line (line 4) in menu.py.

The file currently has:
```
import nuke
import node_layout
import node_layout_prefs_dialog
```

It must become:
```
import nuke
import node_layout
import node_layout_prefs_dialog
import make_room
```

The six Make Room addCommand calls at lines 87-119 all reference `make_room.make_room(...)` as string expressions evaluated by Nuke, but the module was never imported into the menu.py namespace.
  </action>
  <verify>
    <automated>grep -n "import make_room" menu.py | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import make_room" menu.py` returns 1
    - `python3 -c "import ast; ast.parse(open('menu.py').read()); print('OK')"` prints OK (no syntax errors)
  </acceptance_criteria>
  <done>menu.py contains `import make_room` and parses without syntax errors</done>
</task>

</tasks>

<verification>
- `grep "import make_room" menu.py` shows the import line
- `python3 -c "import ast; ast.parse(open('menu.py').read()); print('OK')"` confirms valid syntax
</verification>

<success_criteria>
The NameError from UAT test 5 is resolved: `make_room` is importable in menu.py's namespace.
</success_criteria>

<output>
After completion, create `.planning/phases/16-layout-integration/16-03-SUMMARY.md`
</output>
