---
phase: 11.1-fix-horizontal-layout-functionality
plan: 01
subsystem: testing
tags: [unittest, tdd, red-tests, geometry, horizontal-layout]

# Dependency graph
requires:
  - phase: 11-horizontal-b-spine-layout
    provides: place_subtree_horizontal, _find_or_create_output_dot, horizontal anchor formula
provides:
  - TestLeftExtentOverlap: 2 failing RED tests accepting Bug 1 left-extent overlap fix
  - TestDotYAlignment: 2 failing RED tests + 1 passing guard accepting Bug 2 dot Y alignment fix
affects: [11.1-02-PLAN, 11.1-03-PLAN]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD RED scaffold: broken-formula test pattern — call current (buggy) formula, assert correct result fails"
    - "No-consumer guard test: regression protection for no-change path inside buggy function"

key-files:
  created: []
  modified:
    - tests/test_horizontal_layout.py

key-decisions:
  - "TestLeftExtentOverlap tests use place_subtree_horizontal directly with broken spine_x — avoids layout_upstream/layout_selected full-stack call complexity while still exercising the actual geometry bug"
  - "TestDotYAlignment reuse-check test pre-wires existing_dot (has_output_dot_knob=True) to trigger the lines ~387-390 reuse path, not the new-dot path"
  - "test_no_consumer_returns_none is expected to PASS RED — it is a regression guard for the consumer=None early-return path that must not change"

patterns-established:
  - "Pattern: broken-formula RED test — replicate current broken formula, then assert correct postcondition to create a RED test that goes GREEN only when the formula is fixed"

requirements-completed: []

# Metrics
duration: 8min
completed: 2026-03-16
---

# Phase 11.1 Plan 01: Add RED Regression Tests for Bug 1 and Bug 2 Summary

**Failing TDD scaffold for left-extent overlap (Bug 1) and dot Y misalignment (Bug 2) in horizontal B-spine layout**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-16T10:04:00Z
- **Completed:** 2026-03-16T10:12:13Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Added `TestLeftExtentOverlap` class with 2 failing RED tests confirming Bug 1: leftmost spine node (n.xpos=170) fails to meet required clearance (>= 830) from consumer right edge when broken `spine_x` formula is used
- Added `TestDotYAlignment` class with 2 failing RED tests + 1 passing guard confirming Bug 2: dot is placed at y=192 (below root) but must be at y=308 (centred on consumer)
- All 24 prior tests remain green; full suite shows exactly 4 failures on the new classes

## Task Commits

Each task was committed atomically:

1. **Task 1: Add TestLeftExtentOverlap — failing RED tests for Bug 1** - `9ec83f1` (test)
2. **Task 2: Add TestDotYAlignment — failing RED tests for Bug 2** - `16179cc` (test)

## Files Created/Modified

- `tests/test_horizontal_layout.py` — Added `TestLeftExtentOverlap` (lines ~1121-1265) and `TestDotYAlignment` (lines ~1267-1395) after `TestPlaceOutputDotForHorizontalRootReplay`

## Decisions Made

- `TestLeftExtentOverlap` calls `place_subtree_horizontal` directly with the broken `spine_x` formula (replicating what `layout_upstream` / `layout_selected` currently do) rather than calling the full entry-point functions. This avoids the need to stub state machinery (`write_node_state` requires `nuke.String_Knob`, `nuke.INVISIBLE`) while directly testing the geometry assertion.
- For `TestDotYAlignment`, the reuse-check path test pre-wires `_StubDotNode(has_output_dot_knob=True)` at `consumer.input(0)` before calling `_find_or_create_output_dot`. This triggers the early-return path at lines ~383-391 (the "already wired dot" case).
- `test_no_consumer_returns_none` is designed to PASS immediately (the no-consumer guard is correct and must not change) — documented as a regression guard per plan spec.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The `test_scale_nodes_axis.TestExpandPushAway` errors in the full `discover` run (4 errors) are pre-existing and unrelated to this plan's changes — verified by confirming they exist on the previous commit as well.

## Next Phase Readiness

- Plan 11.1-02 can now implement Bug 1 fix: update `spine_x` formula in `layout_upstream` and `layout_selected` to account for full leftward extent of spine
- Plan 11.1-03 can implement Bug 2 fix: update dot Y in all 3 placement paths in `_find_or_create_output_dot` and `_place_output_dot_for_horizontal_root`
- When both fixes applied, `TestLeftExtentOverlap` and `TestDotYAlignment` new-dot/reuse-check tests go GREEN; `test_no_consumer_returns_none` stays GREEN

---
*Phase: 11.1-fix-horizontal-layout-functionality*
*Completed: 2026-03-16*
