---
phase: 11.1-fix-horizontal-layout-functionality
verified: 2026-03-16T10:35:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase 11.1: Fix Horizontal Layout Functionality — Verification Report

**Phase Goal:** Fix two bugs in horizontal B-spine layout — (1) left-extent overlap where horizontal chain's leftmost nodes extend past vertical spine node, (2) dot Y misalignment where output dot is placed below root instead of at consumer's Y level. Also close 4 debug documents from Phase 11 UAT.
**Verified:** 2026-03-16T10:35:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                         | Status     | Evidence                                                                                           |
|----|---------------------------------------------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------|
| 1  | A 3-node horizontal chain embedded in a vertical spine has its leftmost node to the right of the consumer     | VERIFIED   | `leftward_extent` formula present at lines 1339-1344 (layout_upstream) and 1517-1522 (layout_selected); `TestLeftExtentOverlap` — 2 tests GREEN |
| 2  | The output dot's Y position centres on the consumer node's Y so the connecting wire is horizontal             | VERIFIED   | Consumer-aligned `dot_y` formula at lines 388-389, 412-413 (_find_or_create_output_dot), and 473-474 (_place_output_dot_for_horizontal_root); `TestDotYAlignment` — 2 tests GREEN |
| 3  | Standalone horizontal chains (no downstream consumer) are unaffected — dot still placed below root            | VERIFIED   | All three dot-placement sites use `if consumer_node is not None:` conditional; `else` branch preserves original `root.ypos() + root.screenHeight() + dot_gap`; `test_no_consumer_returns_none` GREEN |
| 4  | All 4 debug documents show status: resolved with commit hash recorded                                         | VERIFIED   | `grep status/verification` confirms all 4 files have `status: resolved` and `verification: applied — fixed by commit <hash>` |
| 5  | TestLeftExtentOverlap and TestDotYAlignment exist as regression tests                                         | VERIFIED   | Both classes present in `tests/test_horizontal_layout.py` (lines 1129 and 1293) |
| 6  | All prior 24+ tests in test_horizontal_layout remain GREEN                                                    | VERIFIED   | `python3 -m unittest tests.test_horizontal_layout -v` — Ran 29 tests, 0 failures, 0 errors |
| 7  | Full test suite shows no new regressions                                                                       | VERIFIED   | `python3 -m unittest discover -s tests -v` — 266 tests, 4 errors (all pre-existing in `test_scale_nodes_axis.TestExpandPushAway`, unrelated to this phase) |

**Score:** 7/7 truths verified

---

## Required Artifacts

### Plan 02 Artifacts (node_layout.py bug fixes)

| Artifact         | Expected                                           | Status   | Details                                                                                             |
|------------------|----------------------------------------------------|----------|-----------------------------------------------------------------------------------------------------|
| `node_layout.py` | spine_x left-extent pre-computation (Bug 1)        | VERIFIED | `leftward_extent` at lines 1339-1344 (layout_upstream) and 1517-1522 (layout_selected); full walk of spine_set using `id()` comparisons |
| `node_layout.py` | dot_y consumer-aligned in 3 placement paths (Bug 2) | VERIFIED | `consumer_node.ypos() + (consumer_node.screenHeight() - dot.screenHeight()) // 2` at lines 389, 413, 474 |

### Plan 01 Artifacts (regression tests)

| Artifact                            | Expected                                   | Status   | Details                                                                   |
|-------------------------------------|--------------------------------------------|----------|---------------------------------------------------------------------------|
| `tests/test_horizontal_layout.py`   | TestLeftExtentOverlap class                | VERIFIED | Lines 1129+; 2 test methods; both GREEN                                   |
| `tests/test_horizontal_layout.py`   | TestDotYAlignment class                    | VERIFIED | Lines 1293+; 2 test methods + 1 guard; all GREEN                          |

### Plan 03 Artifacts (debug document closure)

| Artifact                                                   | Expected               | Status   | Details                                          |
|------------------------------------------------------------|------------------------|----------|--------------------------------------------------|
| `.planning/debug/duplicate-output-dot-on-rerun.md`         | status: resolved       | VERIFIED | Line 2: `status: resolved`; line 423: commit `6f4f050` |
| `.planning/debug/no-output-dot-selected-horizontal.md`     | status: resolved       | VERIFIED | Line 2: `status: resolved`; line 41: commit `65f50f8`  |
| `.planning/debug/horiz03-downstream-replay.md`             | status: resolved       | VERIFIED | Line 2: `status: resolved`; line 78: commit `8ef07b9`  |
| `.planning/debug/horiz04-downstream-replay-wrong-position.md` | status: resolved    | VERIFIED | Line 2: `status: resolved`; line 145: commit `6658d7b` |

---

## Key Link Verification

| From                                            | To                                       | Via                                                | Status   | Details                                                        |
|-------------------------------------------------|------------------------------------------|----------------------------------------------------|----------|----------------------------------------------------------------|
| `layout_upstream` (~line 1313 branch)           | `place_subtree_horizontal spine_x`       | `leftward_extent` pre-computation                  | WIRED    | `replay_spine_set` walked; `spine_x = consumer.xpos() + consumer.screenWidth() + step_x + leftward_extent` |
| `layout_selected` (~line 1474 branch)           | `place_subtree_horizontal spine_x`       | `leftward_extent` pre-computation                  | WIRED    | `root_spine_set` walked; identical formula pattern             |
| `_find_or_create_output_dot` reuse-check path   | `consumer_node.ypos()`                   | `dot_y = consumer_node.ypos() + ...` (line 389)   | WIRED    | Conditional present; `else` fallback preserved                 |
| `_find_or_create_output_dot` new-dot path       | `consumer_node.ypos()`                   | `dot_y = consumer_node.ypos() + ...` (line 413)   | WIRED    | Conditional present; `else` fallback preserved                 |
| `_place_output_dot_for_horizontal_root` existing_dot path | `consumer_node.ypos()`       | `existing_dot.setYpos(...)` (line 474)             | WIRED    | Conditional present; `else` fallback preserved                 |

---

## Requirements Coverage

No requirement IDs were declared for this phase (bug-fix insertion). Phase goal is tracked through must-haves only.

---

## Anti-Patterns Found

No TODO, FIXME, HACK, or PLACEHOLDER comments were found in `node_layout.py`. No stub implementations detected. All five fix sites contain substantive conditional logic — no empty handlers or pass-through stubs.

---

## Commits Verified

All 6 expected commits from Plans 01-03 are present in git history:

| Commit    | Description                                                              |
|-----------|--------------------------------------------------------------------------|
| `9ec83f1` | test(11.1-01): add failing RED tests for Bug 1 — TestLeftExtentOverlap  |
| `16179cc` | test(11.1-01): add failing RED tests for Bug 2 — TestDotYAlignment      |
| `8e90875` | fix(11.1-02): fix Bug 1 — spine_x left-extent pre-computation           |
| `7897aaf` | fix(11.1-02): fix Bug 2 — dot Y aligned to consumer                     |
| `98599c9` | docs(11.1-03): close 4 debug documents                                   |
| `8869e46` | docs(11.1-03): complete plan summary and state update                    |

---

## Human Verification Required

None. All bug fixes are geometry calculations verifiable through unit tests. The tests exercise the exact coordinate postconditions specified in the bug reports. No UI/visual/real-time behavior that requires manual inspection.

---

## Summary

Phase 11.1 goal is fully achieved. Both geometry bugs are fixed at all required code sites (5 targeted changes in node_layout.py), verified by regression tests that pass GREEN (29/29 in test_horizontal_layout). All 4 Phase 11 UAT debug documents are closed with correct commit references. No regressions introduced in the broader test suite — the 4 pre-existing errors in test_scale_nodes_axis.TestExpandPushAway predate this phase and are unrelated.

---

_Verified: 2026-03-16T10:35:00Z_
_Verifier: Claude (gsd-verifier)_
