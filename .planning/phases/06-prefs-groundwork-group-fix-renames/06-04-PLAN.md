---
phase: 06-prefs-groundwork-group-fix-renames
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - node_layout.py
  - tests/test_group_context.py
autonomous: true
requirements:
  - LAYOUT-04
gap_closure: true

must_haves:
  truths:
    - "Dot nodes created by layout commands land inside the Group when the user is viewing it via Group View (not just Ctrl-Enter)"
    - "push_nodes_to_make_room() operates on the correct group's nodes when the Group is open via Group View"
    - "All AST tests in test_group_context.py pass after the fix"
  artifacts:
    - path: "node_layout.py"
      provides: "layout_upstream() and layout_selected() using nuke.lastHitGroup() at capture point"
      contains: "nuke.lastHitGroup()"
    - path: "tests/test_group_context.py"
      provides: "Updated AST tests asserting lastHitGroup() instead of thisGroup()"
      contains: "nuke.lastHitGroup()"
  key_links:
    - from: "layout_upstream() / layout_selected()"
      to: "with current_group:"
      via: "nuke.lastHitGroup() captured before any other Nuke API call"
      pattern: "current_group = nuke.lastHitGroup()"
---

<objective>
Fix the Group View context bug: replace nuke.thisGroup() with nuke.lastHitGroup() in both layout entry points, and update the AST tests that assert the capture-text to match the new call.

Purpose: nuke.thisGroup() resolves the Python call-stack context (only correct after Ctrl-Enter). nuke.lastHitGroup() resolves the UI panel context (correct for both Ctrl-Enter and Group View). Foundry documents lastHitGroup() explicitly for this use case and nukescripts.createNodeLocal uses it internally.

Output: Two lines changed in node_layout.py; two tests updated in test_group_context.py; all 168+ tests pass.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prefs-groundwork-group-fix-renames/06-03-SUMMARY.md
@.planning/debug/group-view-context-scoping.md

<interfaces>
<!-- Key facts confirmed from codebase — executor needs no further exploration. -->

From node_layout.py (lines to change):
  line 583:  current_group = nuke.thisGroup()    # MUST be the first Nuke API call
  line 633:  current_group = nuke.thisGroup()    # MUST be the first Nuke API call

Both must become:
  current_group = nuke.lastHitGroup()    # MUST be the first Nuke API call

Comments after the call can remain, but update the comment to say lastHitGroup if desired for clarity.

From tests/test_group_context.py — two tests that contain the literal "current_group = nuke.thisGroup()":
  test_layout_upstream_captures_current_group_first  (line 43: capture_text = "current_group = nuke.thisGroup()")
  test_layout_selected_captures_current_group_first  (line 99: capture_text = "current_group = nuke.thisGroup()")

Both must assert "current_group = nuke.lastHitGroup()" instead.

The module docstring on line 6-8 also says "captures nuke.thisGroup()" twice — update both to "captures nuke.lastHitGroup()".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace nuke.thisGroup() with nuke.lastHitGroup() in layout entry points and update AST tests</name>
  <files>node_layout.py, tests/test_group_context.py</files>
  <behavior>
    - After the change, node_layout.py must NOT contain "nuke.thisGroup()" anywhere in layout_upstream() or layout_selected()
    - After the change, node_layout.py MUST contain "nuke.lastHitGroup()" at line 583 and line 633 (give or take minor line shifts)
    - test_layout_upstream_captures_current_group_first must assert capture_text = "current_group = nuke.lastHitGroup()"
    - test_layout_selected_captures_current_group_first must assert capture_text = "current_group = nuke.lastHitGroup()"
    - All existing tests (168+) must continue to pass — no regressions
  </behavior>
  <action>
    Step 1 — Edit node_layout.py:
    Find the two occurrences of:
      current_group = nuke.thisGroup()    # MUST be the first Nuke API call
    Replace each with:
      current_group = nuke.lastHitGroup()    # MUST be the first Nuke API call
    There are exactly two occurrences: one in layout_upstream() and one in layout_selected(). Do not change any other lines.

    Step 2 — Edit tests/test_group_context.py:
    Update the module docstring (lines 6-8) from "captures nuke.thisGroup()" to "captures nuke.lastHitGroup()" in both places.
    In test_layout_upstream_captures_current_group_first: change capture_text value from "current_group = nuke.thisGroup()" to "current_group = nuke.lastHitGroup()".
    In test_layout_selected_captures_current_group_first: change capture_text value from "current_group = nuke.thisGroup()" to "current_group = nuke.lastHitGroup()".
    No other test logic changes — the ordering assertions, the "with current_group:" assertions, and the push_nodes tests are all correct as-is.

    Step 3 — Verify no stray thisGroup() references remain in either file:
      grep "thisGroup" /workspace/node_layout.py
      grep "thisGroup" /workspace/tests/test_group_context.py
    Both greps must return empty.

    Rationale (do not deviate): nuke.thisGroup() returns the group from the Python call stack — it only works when the user Ctrl-Entered into a Group. nuke.lastHitGroup() returns the group from the last-clicked Group View panel — it works for both Ctrl-Enter and Group View. Foundry documents this as the canonical API for menu commands. When at root (no Group View active), lastHitGroup() returns nuke.root(), the same safe fallback as thisGroup().
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/test_group_context.py -v && python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - node_layout.py contains "nuke.lastHitGroup()" at both entry points and zero occurrences of "nuke.thisGroup()"
    - tests/test_group_context.py asserts "current_group = nuke.lastHitGroup()" in both ordering tests
    - All tests pass (no regressions from the 168 baseline)
  </done>
</task>

</tasks>

<verification>
After the task completes:
  grep -n "lastHitGroup\|thisGroup" /workspace/node_layout.py
  — should show lastHitGroup at two lines, zero thisGroup lines

  grep -n "lastHitGroup\|thisGroup" /workspace/tests/test_group_context.py
  — should show lastHitGroup in docstring (2×) and in both capture_text strings (2×), zero thisGroup lines

  python -m pytest tests/ -q
  — all tests pass
</verification>

<success_criteria>
- layout_upstream() and layout_selected() both capture `current_group = nuke.lastHitGroup()` as their first Nuke API call
- nuke.thisGroup() is absent from both functions
- All AST tests in test_group_context.py pass (they now assert the correct API name)
- Full test suite passes with no regressions
- Group View context gap from UAT test 4 is closed
</success_criteria>

<output>
After completion, create `.planning/phases/06-prefs-groundwork-group-fix-renames/06-04-SUMMARY.md`
</output>
