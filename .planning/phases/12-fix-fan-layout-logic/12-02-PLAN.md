---
phase: 12-fix-fan-layout-logic
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - node_layout.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Fan Dot row Y is above the consumer top (dot.ypos() < consumer.ypos())"
    - "Fan Dot bottom edge clears consumer top by at least snap_threshold-1 pixels"
    - "A1's X position starts at or after B's subtree right edge when B is wide"
    - "compute_dims fan W includes B's rightward overhang so bbox callers get correct width"
    - "All 11 fan tests pass (including the 3 new RED tests from Plan 01)"
    - "Full test suite shows no regressions"
  artifacts:
    - path: "node_layout.py"
      provides: "3 arithmetic fixes in place_subtree fan branch and compute_dims fan W formula"
      contains: "snap_threshold - 1"
  key_links:
    - from: "node_layout.py place_subtree fan branch"
      to: "gap_to_fan reserved space"
      via: "dot_row_y = y - (snap_threshold - 1) - inp.screenHeight()"
      pattern: "snap_threshold - 1.*screenHeight"
    - from: "node_layout.py place_subtree fan branch"
      to: "x_positions[non_mask_start]"
      via: "max(x + node.screenWidth(), x_positions[non_mask_start] + child_dims[non_mask_start][0])"
      pattern: "x_positions\\[non_mask_start\\].*child_dims"
    - from: "node_layout.py compute_dims"
      to: "fan W formula"
      via: "b_right_overhang = max(0, (b_w - node.screenWidth()) // 2)"
      pattern: "b_right_overhang"
---

<objective>
Apply 3 arithmetic fixes in node_layout.py to turn the 3 RED tests from Plan 01 GREEN.

Purpose: Correct fan layout geometry so Dot rows land in the reserved gap above the consumer (not on it), A1 clears B's wide subtree right edge, and compute_dims correctly reports fan bbox width.
Output: node_layout.py with all 3 fix sites updated; all 11 fan tests pass; full suite green.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-fan-layout-logic/12-CONTEXT.md
@.planning/phases/12-fix-fan-layout-logic/12-RESEARCH.md
@.planning/phases/12-fix-fan-layout-logic/12-01-SUMMARY.md

<interfaces>
<!-- Fix sites in node_layout.py — confirmed by direct code read. -->

Fix Site 1 — place_subtree fan Dot row Y (line ~1145):
Current (BUGGY):
  if fan_active and n >= 3:
      # Fan mode: uniform Dot row — all Dots at the same Y regardless of index.
      dot_row_y = y + (node.screenHeight() - inp.screenHeight()) // 2
      dot_y = dot_row_y

Corrected:
  if fan_active and n >= 3:
      # Fan mode: Dot row sits in gap above consumer, symmetric snap_threshold-1 margins.
      dot_row_y = y - (snap_threshold - 1) - inp.screenHeight()
      dot_y = dot_row_y

Fix Site 2 — place_subtree A1/A2 X start (line ~1074):
Current (BUGGY):
  current_x = x + node.screenWidth() + (side_margins_h[non_mask_start + 1] if non_mask_start + 1 < n else 0)

Corrected:
  current_x = max(x + node.screenWidth(), x_positions[non_mask_start] + child_dims[non_mask_start][0]) + (side_margins_h[non_mask_start + 1] if non_mask_start + 1 < n else 0)

Note: x_positions[non_mask_start] is set at line 1072 (B centered above consumer) before this line runs — it is already available.

Fix Site 3 — compute_dims fan W formula (lines ~896-904):
Current (BUGGY) fan-with-mask path:
  non_mask_dims = child_dims[mask_count:]
  W = max(non_mask_dims[0][0],
          node.screenWidth() + sum(side_margins_h[mask_count + 1:]) + sum(w for w, h in non_mask_dims[1:]))

Corrected fan-with-mask path:
  non_mask_dims = child_dims[mask_count:]
  b_w = non_mask_dims[0][0]
  b_right_overhang = max(0, (b_w - node.screenWidth()) // 2)
  W = max(b_w,
          node.screenWidth() + b_right_overhang + sum(side_margins_h[mask_count + 1:]) + sum(w for w, h in non_mask_dims[1:]))

Current (BUGGY) fan-without-mask path (same structural bug):
  W = max(child_dims[0][0],
          node.screenWidth() + sum(side_margins_h[1:]) + sum(w for w, h in child_dims[1:]))

Corrected fan-without-mask path:
  b_w = child_dims[0][0]
  b_right_overhang = max(0, (b_w - node.screenWidth()) // 2)
  W = max(b_w,
          node.screenWidth() + b_right_overhang + sum(side_margins_h[1:]) + sum(w for w, h in child_dims[1:]))

Integer arithmetic rule: use // (not /) for overhang — must match _center_x which uses //.
Nuke Y axis: positive Y is DOWN; y - N moves UPWARD (toward upstream inputs).
Do NOT change the staircase formula at lines 1147-1152 (elif i == n - 1 branch).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Bug 1 — fan Dot row Y formula in place_subtree</name>
  <files>node_layout.py</files>
  <action>
    Locate the fan Dot row Y block in place_subtree (search for "Fan mode: uniform Dot row"). It looks like:

      if fan_active and n >= 3:
          # Fan mode: uniform Dot row — all Dots at the same Y regardless of index.
          dot_row_y = y + (node.screenHeight() - inp.screenHeight()) // 2
          dot_y = dot_row_y

    Replace with:

      if fan_active and n >= 3:
          # Fan mode: Dot row sits in gap above consumer, symmetric snap_threshold-1 margins.
          # gap_to_fan is already sized to hold the Dot; place bottom of Dot at snap_threshold-1
          # above consumer top (y). Nuke Y is positive-down so subtract to move upward.
          dot_row_y = y - (snap_threshold - 1) - inp.screenHeight()
          dot_y = dot_row_y

    Do NOT modify the elif i == n - 1 staircase branch immediately below (lines 1147-1149). Only the fan_active block changes.

    After the edit: run the Bug 1 test to confirm it turns GREEN.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest tests.test_fan_alignment.TestPlaceSubtreeFanRoots.test_fan_dot_row_y_in_gap_not_on_consumer -v 2>&amp;1 | tail -5</automated>
  </verify>
  <done>test_fan_dot_row_y_in_gap_not_on_consumer PASSES. test_fan_dot_row_uniform_y still passes (Dots remain at the same Y row — just at the correct Y now).</done>
</task>

<task type="auto">
  <name>Task 2: Fix Bug 2 — A1/A2 X start and compute_dims W overhang</name>
  <files>node_layout.py</files>
  <action>
    **Fix Site 2 — place_subtree A1 X:**

    Locate the fan A1/A2 X-positioning block (search for "A1, A2, ... step rightward from consumer's right edge"). It looks like:

      # A1, A2, ... step rightward from consumer's right edge.
      current_x = x + node.screenWidth() + (side_margins_h[non_mask_start + 1] if non_mask_start + 1 < n else 0)

    Replace with:

      # A1, A2, ... step rightward from max(consumer right, B subtree right).
      # When B is wider than the consumer it overhangs rightward; A1 must clear B's right edge.
      current_x = max(x + node.screenWidth(), x_positions[non_mask_start] + child_dims[non_mask_start][0]) + (side_margins_h[non_mask_start + 1] if non_mask_start + 1 < n else 0)

    x_positions[non_mask_start] is already assigned on the line above (B centered above consumer) so it is in scope.

    **Fix Site 3 — compute_dims W formula (both fan paths):**

    Locate the fan W section in compute_dims (search for "n >= 3: W formula differs in fan mode"). It looks like:

      if fan_active and mask_count > 0:
          # Fan mode with mask: mask is placed LEFT; W measures only rightward (non-mask) spread.
          non_mask_dims = child_dims[mask_count:]
          W = max(non_mask_dims[0][0],
                  node.screenWidth() + sum(side_margins_h[mask_count + 1:]) + sum(w for w, h in non_mask_dims[1:]))
      else:
          # Standard n >= 3: input[0] centered above node; inputs[1..n-1] rightward from node's right edge
          W = max(child_dims[0][0],
                  node.screenWidth() + sum(side_margins_h[1:]) + sum(w for w, h in child_dims[1:]))

    Replace with:

      if fan_active and mask_count > 0:
          # Fan mode with mask: mask is placed LEFT; W measures rightward (non-mask) spread.
          # B (first non-mask) is centered above consumer; if B is wider than consumer it overhangs
          # rightward past consumer's right edge by (b_w - node_w) // 2. A1 must clear B's right edge.
          non_mask_dims = child_dims[mask_count:]
          b_w = non_mask_dims[0][0]
          b_right_overhang = max(0, (b_w - node.screenWidth()) // 2)
          W = max(b_w,
                  node.screenWidth() + b_right_overhang + sum(side_margins_h[mask_count + 1:]) + sum(w for w, h in non_mask_dims[1:]))
      else:
          # n >= 3 fan without mask (or non-fan fallback): same B-overhang correction.
          b_w = child_dims[0][0]
          b_right_overhang = max(0, (b_w - node.screenWidth()) // 2)
          W = max(b_w,
                  node.screenWidth() + b_right_overhang + sum(side_margins_h[1:]) + sum(w for w, h in child_dims[1:]))

    Use // (integer division) for b_right_overhang — matches _center_x which uses //.

    After both edits: run the full fan alignment suite.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest tests.test_fan_alignment -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>All 11 fan tests PASS. No failures, no errors. Output: "Ran 11 tests in X.XXXs OK".</done>
</task>

<task type="auto">
  <name>Task 3: Full regression — confirm no other tests broken</name>
  <files></files>
  <action>
    Run the complete test suite to confirm no regressions across all test files. Report any failures.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest discover -s /workspace/tests -v 2>&amp;1 | tail -10</automated>
  </verify>
  <done>Full suite PASSES. Output ends with "OK" (or lists only pre-existing known failures if any). All fan tests green; no new failures introduced by the 3 fix sites.</done>
</task>

</tasks>

<verification>
After Wave 2 complete:
- python3 -m unittest tests.test_fan_alignment -v → 11 tests, 0 failures, 0 errors
- python3 -m unittest discover -s /workspace/tests -v → full suite green
- node_layout.py contains dot_row_y = y - (snap_threshold - 1) - inp.screenHeight() in the fan branch
- node_layout.py contains max(x + node.screenWidth(), x_positions[non_mask_start] + child_dims[non_mask_start][0]) at the A1 X start line
- node_layout.py contains b_right_overhang = max(0, (b_w - node.screenWidth()) // 2) in compute_dims fan W (both paths)
</verification>

<success_criteria>
node_layout.py has 3 targeted fixes applied. All 11 tests in test_fan_alignment.py pass. Full test suite passes. The 3 previously RED tests are now GREEN.
</success_criteria>

<output>
After completion, create .planning/phases/12-fix-fan-layout-logic/12-02-SUMMARY.md
</output>
