---
phase: 12-fix-fan-layout-logic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_fan_alignment.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "test_fan_dot_row_y_in_gap_not_on_consumer exists and FAILS before the fix"
    - "test_fan_a1_x_clears_b_subtree_right_edge exists and FAILS before the fix"
    - "test_compute_dims_fan_w_accounts_for_b_overhang exists and FAILS before the fix"
    - "All 8 existing fan tests continue to pass (no regression from scaffold additions)"
  artifacts:
    - path: "tests/test_fan_alignment.py"
      provides: "3 new failing test methods covering Bug 1 Y, Bug 2 X, and compute_dims W"
      contains: "test_fan_dot_row_y_in_gap_not_on_consumer"
  key_links:
    - from: "tests/test_fan_alignment.py"
      to: "node_layout.place_subtree fan branch"
      via: "nl.place_subtree() direct call"
      pattern: "nl\\.place_subtree"
    - from: "tests/test_fan_alignment.py"
      to: "node_layout.compute_dims fan branch"
      via: "nl.compute_dims() direct call"
      pattern: "nl\\.compute_dims"
---

<objective>
Write 3 failing RED regression tests for the two fan layout geometry bugs before any fixes are applied.

Purpose: Establish acceptance criteria for Plan 02 fixes. Tests must FAIL now and PASS after Plan 02.
Output: 3 new test methods added to tests/test_fan_alignment.py; all 3 are RED (failing) on the current codebase.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-fan-layout-logic/12-CONTEXT.md
@.planning/phases/12-fix-fan-layout-logic/12-RESEARCH.md
@.planning/phases/12-fix-fan-layout-logic/12-VALIDATION.md

<interfaces>
<!-- Key patterns from tests/test_fan_alignment.py (existing scaffold). -->

Existing test classes in tests/test_fan_alignment.py:
- TestFanActivePredicate (2 tests)
- TestComputeDimsFanHeight (1 test)
- TestPlaceSubtreeFanRoots (2 tests: test_fan_roots_same_y, test_fan_dot_row_uniform_y)
- TestMaskSideSwap (3 tests)

Key stubs available at module level:
- _StubNode(width=80, height=28, xpos=0, ypos=0, node_class="Grade")
- _StubMergeNode(**kwargs) — Merge2, inputLabel returns "B"/"A"/"M" for slots 0/1/2
- nl = the node_layout module (loaded via importlib)
- _reset_prefs() — restores prefs to DEFAULTS

Test setUp pattern:
  def setUp(self):
      _reset_prefs()
      self.memo = {}
      self.snap_threshold = 8
      self.node_count = 5

place_subtree signature:
  nl.place_subtree(node, x, y, memo, snap_threshold, node_count)

compute_dims signature:
  nl.compute_dims(node, memo, snap_threshold, node_count)

Nuke Y axis: positive Y is DOWN. Upstream nodes are at LOWER Y values (negative direction).
Consumer at ypos=500 means inputs are placed at Y < 500.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Bug 1 test — fan Dot row Y must be above consumer, not on it</name>
  <files>tests/test_fan_alignment.py</files>
  <behavior>
    - test_fan_dot_row_y_in_gap_not_on_consumer: place_subtree on a 3-input fan consumer at ypos=500; collect all Dot nodes in non-mask slots; assert each dot.ypos() &lt; 500 (dot is above consumer top in positive-down Y); assert dot_bottom = dot.ypos() + dot.screenHeight() &lt;= 500 - (snap_threshold - 1) = 493 (dot bottom clears consumer top by at least snap_threshold-1). Current buggy formula places dot AT ypos=500+something, so this test FAILS RED.
  </behavior>
  <action>
    Add method test_fan_dot_row_y_in_gap_not_on_consumer to the existing TestPlaceSubtreeFanRoots class.

    Use the existing _build_three_input_consumer() helper. Call nl.place_subtree(consumer, 500, 500, self.memo, self.snap_threshold, self.node_count). Collect Dot nodes from non-mask slots (slots 0, 1, 3) in consumer._inputs where Class() == "Dot". Assert len(dot_nodes) >= 3. For each dot: assertLess(dot.ypos(), 500) and assertLessEqual(dot.ypos() + dot.screenHeight(), 500 - (self.snap_threshold - 1)).

    Do NOT modify the staircase formula path. Do NOT change any existing tests.

    After adding: run the test to confirm it FAILS. The current dot_row_y formula is y + (node.screenHeight() - inp.screenHeight()) // 2 = 500 + (28 - 12) // 2 = 508, which is greater than 500, so assertLess(508, 500) fails — correct RED state.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest tests.test_fan_alignment.TestPlaceSubtreeFanRoots.test_fan_dot_row_y_in_gap_not_on_consumer -v 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Test exists and outputs FAIL (not ERROR). Existing 8 tests still pass: python3 -m unittest tests.test_fan_alignment -v shows 9 tests, 1 failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Bug 2 test + compute_dims W test</name>
  <files>tests/test_fan_alignment.py</files>
  <behavior>
    - test_fan_a1_x_clears_b_subtree_right_edge: consumer width=80 at xpos=500; B stub width=500 (wide — triggers overhang); A1 stub width=80; A2 stub width=80; consumer._inputs = [input_b, input_a1, None, input_a2]. After place_subtree: B is centered above consumer so B left = 500 + (80-500)//2 = 290, B right = 790. Assert input_a1.xpos() >= 790 (A1 must clear B's right edge). Current code places A1 at x + node.screenWidth() + margin = 500 + 80 + margin = ~588, which is less than 790 — FAILS RED.
    - test_compute_dims_fan_w_accounts_for_b_overhang: consumer width=80; B stub width=500; A1 stub width=80; A2 stub width=80; consumer._inputs = [input_b, input_a1, None, input_a2]. After compute_dims: W must be >= B_right_overhang + consumer_width + margins + A1_width. Specifically W must account for (500 - 80) // 2 = 210 px of B overhang. Assert W >= 500 + 80 + some margin (effectively W > 590). Current formula ignores B overhang so W will be ~= 80 + margins + 80 — FAILS RED.
  </behavior>
  <action>
    Add test_fan_a1_x_clears_b_subtree_right_edge to TestPlaceSubtreeFanRoots class.

    Setup: input_b = _StubNode(width=500, height=28); input_a1 = _StubNode(width=80, height=28); input_a2 = _StubNode(width=80, height=28); consumer = _StubMergeNode(width=80, height=28, xpos=500, ypos=500); consumer._inputs = [input_b, input_a1, None, input_a2]. Call nl.place_subtree(consumer, 500, 500, self.memo, self.snap_threshold, self.node_count). Read input_a1.xpos(). Compute b_left = input_b.xpos(); b_right = b_left + 500. assertGreaterEqual(input_a1.xpos(), b_right, "A1 must start at or past B subtree right edge").

    Add new class TestComputeDimsFanWidth after TestPlaceSubtreeFanRoots:
      - setUp: _reset_prefs(); self.memo = {}; self.snap_threshold = 8; self.node_count = 5
      - test_compute_dims_fan_w_accounts_for_b_overhang: input_b = _StubNode(width=500, height=28); input_a1 = _StubNode(width=80, height=28); input_a2 = _StubNode(width=80, height=28); consumer = _StubMergeNode(width=80, height=28, xpos=500, ypos=500); consumer._inputs = [input_b, input_a1, None, input_a2]. Call nl.compute_dims(consumer, self.memo, self.snap_threshold, self.node_count). Assert W >= 500 + 80 + (some gap > 0) by assertGreater(width, 500 + 80), i.e., assertGreater(width, 580). The B overhang is (500 - 80) // 2 = 210; correct W includes 80 + 210 + margin + 80 = ~590+. Current formula gives W = max(500, 80 + margin + 80) = ~500 which just barely passes or fails depending on margins — to be safe assert width > 580 with message explaining the overhang is missing.

    Note: For test_compute_dims_fan_w_accounts_for_b_overhang, the assert must be something the current code provably fails. The current formula is max(non_mask_dims[0][0], node_w + sum(margins) + sum(A_widths)) = max(500, 80 + margin + 80). With default margins ~7px this gives max(500, ~167) = 500. The correct value should be max(500, 80 + 210 + margin + 80) = max(500, ~377) which still gives 500. Reconsider: fan-without-mask path (lines 902-904): W = max(child_dims[0][0], node.screenWidth() + sum(side_margins_h[1:]) + sum(w for w, h in child_dims[1:])). For 3 inputs (no mask): max(500, 80 + margin_1 + margin_2 + 80 + 80) ~ max(500, 254). Result = 500. After fix, overhang_b = (500-80)//2 = 210, W = max(500, 80 + 210 + margin + 80 + 80) = max(500, ~464) = 500 — still 500! The fix does not change W when B subtree width already dominates.

    Re-think: The real impact is W should be B_left_offset + B_width when B overhangs rightward past consumer's right edge AND A inputs extend past that. The W formula needs to account for the fact that A1 now starts at B_right rather than consumer_right. Effective W = max(b_w, b_right_overhang + node_w + margins + a_widths). For our numbers: max(500, 210 + 80 + margin + 80 + 80) = max(500, ~464). With default margin ~7: max(500, 457) = 500. So W = 500 both before and after the formula fix for this particular test configuration.

    This means the test_compute_dims_fan_w_accounts_for_b_overhang assertion must be crafted so the FIXED formula gives a larger value than the BROKEN formula AND both differ from B_width. Use A widths large enough so the sum exceeds B alone: input_a1 = _StubNode(width=300); input_a2 = _StubNode(width=300). Then: broken W = max(500, 80 + margin + 300 + 300) ~ max(500, 687) = 687. Fixed W = max(500, 210 + 80 + margin + 300 + 300) ~ max(500, 897) = 897. Assert width > 687 to prove the overhang was added. The broken formula gives ~687; assert width >= 897 (or assertGreater(width, 880)).

    Use: input_b = _StubNode(width=500, height=28); input_a1 = _StubNode(width=300, height=28); input_a2 = _StubNode(width=300, height=28). assertGreater(width, 880, "W must include B right overhang ((500-80)//2=210 px) + node_w + margins + A_widths"). Broken formula gives ~687 (fails); fixed formula gives ~897 (passes).

    After adding both new tests: run full fan suite to confirm 2 new failures and 8 existing passes.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest tests.test_fan_alignment -v 2>&amp;1 | tail -15</automated>
  </verify>
  <done>11 tests total: 8 pass (existing), 3 fail (new RED tests). No ERRORs, only FAILUREs. Output shows FAIL for test_fan_dot_row_y_in_gap_not_on_consumer, test_fan_a1_x_clears_b_subtree_right_edge, test_compute_dims_fan_w_accounts_for_b_overhang.</done>
</task>

</tasks>

<verification>
After Wave 1 complete:
- python3 -m unittest tests.test_fan_alignment -v shows 11 tests: 8 pass, 3 fail (no errors)
- The 3 failing tests specifically name the geometry assertions from Bug 1, Bug 2, compute_dims W
- python3 -m unittest discover -s /workspace/tests -v shows no new failures in other test files
</verification>

<success_criteria>
tests/test_fan_alignment.py contains 11 tests total. 3 new tests FAIL with AssertionError (not Error). 8 existing tests PASS. RED scaffold is complete and ready for Plan 02 fixes.
</success_criteria>

<output>
After completion, create .planning/phases/12-fix-fan-layout-logic/12-01-SUMMARY.md
</output>
