---
phase: 11-horizontal-b-spine-layout
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_horizontal_layout.py
autonomous: true
requirements:
  - HORIZ-01
  - HORIZ-02
  - HORIZ-03

must_haves:
  truths:
    - "tests/test_horizontal_layout.py exists and all tests fail RED (no implementation yet)"
    - "TestHorizontalSpine covers spine X-step placement and multiple-node chain"
    - "TestOutputDot covers Dot creation below root and reuse-on-replay"
    - "TestMaskKink covers single and cumulative kink drops"
    - "TestSideInputPlacement covers A-inputs above spine node (negative Y)"
    - "TestHorizontalAST covers function existence in node_layout.py"
    - "TestModeReplay covers layout_upstream/layout_selected dispatching to horizontal"
  artifacts:
    - path: "tests/test_horizontal_layout.py"
      provides: "RED test scaffold for all HORIZ-01, HORIZ-02, HORIZ-03 behaviors"
      min_lines: 200
  key_links:
    - from: "tests/test_horizontal_layout.py"
      to: "node_layout.py"
      via: "importlib spec_from_file_location (same pattern as test_fan_alignment.py)"
      pattern: "spec_from_file_location.*node_layout"
---

<objective>
Create the RED test scaffold for Phase 11. Every test in the file must fail at this stage because none of the implementation exists yet.

Purpose: Establish the behavioral contract for horizontal B-spine layout before any production code is written (TDD Wave 0).
Output: tests/test_horizontal_layout.py with 10 failing tests across 6 test classes.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-horizontal-b-spine-layout/11-CONTEXT.md
@.planning/phases/11-horizontal-b-spine-layout/11-RESEARCH.md
@.planning/phases/11-horizontal-b-spine-layout/11-VALIDATION.md

<interfaces>
<!-- Nuke stub pattern — from tests/test_fan_alignment.py (use identical stub structure) -->
<!-- NODE_LAYOUT_PATH = "/workspace/node_layout.py" (absolute path import pattern) -->

<!-- State defaults from node_layout_state.py -->
_DEFAULT_STATE = {
    "scheme": "normal",
    "mode": "vertical",   # Phase 11 horizontal path writes "horizontal"
    "h_scale": 1.0,
    "v_scale": 1.0,
}

<!-- Current compute_dims memo key (to be extended in Plan 03): -->
memo_key = (id(node), scheme_multiplier, node_h_scale, node_v_scale)
<!-- Phase 11 extension: -->
memo_key = (id(node), scheme_multiplier, node_h_scale, node_v_scale, layout_mode)

<!-- Functions that MUST exist after Phase 11 (AST tests verify these): -->
place_subtree_horizontal(node, spine_x, spine_y, snap_threshold, node_count,
                         scheme_multiplier=None, per_node_h_scale=None,
                         per_node_v_scale=None, current_prefs=None,
                         current_group=None)
layout_upstream_horizontal(scheme_multiplier=None)
layout_selected_horizontal(scheme_multiplier=None)
_find_or_create_output_dot(root, consumer_node, consumer_slot, current_group)

<!-- Spine step formula (from RESEARCH.md): -->
step_x = horizontal_subtree_gap * scheme_multiplier
spine_x[i+1] = spine_x[i] - step_x - node.screenWidth()

<!-- Mask kink formula: -->
kink_drop = compute_dims(mask_node)[1] + _subtree_margin(spine_node, mask_slot, ...)
cumulative_kink = sum of all upstream kink_drops so far
</interfaces>
</context>

<feature>
  <name>Horizontal B-Spine Layout — RED test scaffold</name>
  <files>tests/test_horizontal_layout.py</files>
  <behavior>
    Tests are structured as 6 classes. Expected RED state: ALL 10 tests FAIL (implementation does not exist yet).

    TestHorizontalAST (AST — no Nuke stub needed):
    - test_place_subtree_horizontal_exists: ast.parse node_layout.py, assert "place_subtree_horizontal" in function names
    - test_layout_upstream_horizontal_exists: assert "layout_upstream_horizontal" in function names
    - test_layout_selected_horizontal_exists: assert "layout_selected_horizontal" in function names

    TestHorizontalSpine (unit — stub Nuke):
    - test_single_node_placed_at_root_x: place_subtree_horizontal on 1-node chain, node.xpos() == root_x
    - test_two_node_spine_step_left: place_subtree_horizontal on root->A chain; A.xpos() < root.xpos() by at least step_x

    TestOutputDot (unit — stub Nuke):
    - test_output_dot_created_below_root: after horizontal layout with downstream consumer, a Dot exists between root and consumer; Dot.ypos() > root.ypos()
    - test_output_dot_reused_on_replay: calling _find_or_create_output_dot twice returns the same Dot object (not a second one)

    TestMaskKink (unit — stub Nuke):
    - test_single_mask_kink_drops_downstream: spine node with mask input causes downstream segment's spine_y to be lower (higher Y value) than upstream segment's spine_y

    TestSideInputPlacement (unit — stub Nuke):
    - test_side_input_above_spine_node: A-input is placed above its spine node — A.ypos() < spine_node.ypos() (lower Y = higher on screen in Nuke)

    TestModeReplay (unit — stub Nuke, AST-assisted):
    - test_layout_upstream_reads_mode_and_dispatches: when stored mode is "horizontal", layout_upstream() must call into horizontal path — verify via mock or AST check that the dispatch branch exists
  </behavior>
  <implementation>
    RED phase only. Write all 10 tests so they fail with AttributeError or AssertionError — the implementation functions do not exist yet.

    File structure:
    1. Module docstring explaining expected RED state and which tests pass/fail
    2. Nuke stub section (copy _StubKnob, _StubNode, stub_nuke pattern from test_fan_alignment.py — identical structure)
    3. Module loader helper (spec_from_file_location for /workspace/node_layout.py)
    4. TestHorizontalAST class — uses ast.parse, no Nuke stub needed, fails because functions missing
    5. TestHorizontalSpine class — uses stub Nuke, calls place_subtree_horizontal
    6. TestOutputDot class — uses stub Nuke, calls _find_or_create_output_dot
    7. TestMaskKink class — uses stub Nuke, calls place_subtree_horizontal with mask node
    8. TestSideInputPlacement class — uses stub Nuke, verifies Y position of side input
    9. TestModeReplay class — AST check that layout_upstream() body contains horizontal dispatch

    Stub Nuke requirements for this file:
    - Same _StubKnob, _StubNode from test_fan_alignment.py
    - _StubNode.setInput(slot, node) and .input(slot) must work (for Dot wiring tests)
    - stub_nuke.nodes.Dot() must return a new _StubNode(width=12, height=12, node_class='Dot')
    - stub_nuke.selectedNode() and stub_nuke.selectedNodes() must be patchable
    - stub_nuke.lastHitGroup() returns None (root context)
    - stub_nuke.allNodes() returns configurable list

    Do NOT import node_layout at module level — use spec_from_file_location inside setUp() to reload fresh per test class.
  </implementation>
</feature>

<verification>
<automated>python3 -m unittest tests/test_horizontal_layout.py -q 2>&1 | tail -5</automated>
Expected RED: all 10 tests fail. If any test passes unexpectedly, document why in the SUMMARY.
</verification>

<success_criteria>
- tests/test_horizontal_layout.py exists with 6 test classes and 10 test methods
- Running the file returns 10 FAILs/ERRORs (no passing tests)
- No import errors from the test file itself (stubs work correctly)
- Full suite `python3 -m unittest discover tests/ -q` still passes all previously-passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-horizontal-b-spine-layout/11-01-SUMMARY.md`
</output>
