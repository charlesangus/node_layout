---
phase: 11-horizontal-b-spine-layout
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - node_layout.py
  - menu.py
autonomous: true
requirements:
  - HORIZ-02
  - HORIZ-03

must_haves:
  truths:
    - "layout_upstream_horizontal() exists in node_layout.py as a full entry point with undo wrapping"
    - "layout_selected_horizontal() exists in node_layout.py as a full entry point with undo wrapping"
    - "layout_upstream() reads stored mode and dispatches to place_subtree_horizontal() when mode == 'horizontal'"
    - "layout_selected() dispatches per-root to horizontal or vertical path based on each root's stored mode"
    - "State write-back records mode='horizontal' on horizontal path and mode='vertical' on vertical path"
    - "compute_dims() accepts layout_mode parameter; memo key is 5-tuple (id, scheme_multiplier, h_scale, v_scale, layout_mode)"
    - "menu.py registers 'Layout Upstream Horizontal' and 'Layout Selected Horizontal' with no keyboard shortcuts"
    - "All 10 tests in test_horizontal_layout.py pass GREEN"
  artifacts:
    - path: "node_layout.py"
      provides: "layout_upstream_horizontal, layout_selected_horizontal, mode dispatch in layout_upstream/layout_selected, compute_dims layout_mode param"
      exports:
        - "layout_upstream_horizontal"
        - "layout_selected_horizontal"
    - path: "menu.py"
      provides: "Layout Upstream Horizontal and Layout Selected Horizontal menu commands"
      contains: "Layout Upstream Horizontal"
  key_links:
    - from: "layout_upstream()"
      to: "place_subtree_horizontal()"
      via: "if root_mode == 'horizontal': dispatch branch"
      pattern: "root_mode.*horizontal|horizontal.*place_subtree"
    - from: "layout_upstream()"
      to: "node_layout_state.write_node_state()"
      via: "stored_state['mode'] = layout_mode_to_write conditional"
      pattern: "layout_mode_to_write"
    - from: "compute_dims"
      to: "memo dict"
      via: "5-tuple inline key (id(node), scheme_multiplier, node_h_scale, node_v_scale, layout_mode)"
      pattern: "node_h_scale.*memo\\[|memo\\[.*node_h_scale"
---

<objective>
Wire the horizontal B-spine entry points, mode dispatch, compute_dims memo key extension, and menu registration. This completes Phase 11 by satisfying HORIZ-02 (layout_selected_horizontal command) and HORIZ-03 (stored mode replay).

Purpose: Connect the core algorithm from Plan 02 to the user-facing command surface and make normal layout commands replay horizontal mode automatically from stored state.
Output: node_layout.py fully wired with entry points + mode dispatch + 5-tuple memo key; menu.py with two new commands.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-horizontal-b-spine-layout/11-CONTEXT.md
@.planning/phases/11-horizontal-b-spine-layout/11-RESEARCH.md
@.planning/phases/11-horizontal-b-spine-layout/11-01-SUMMARY.md
@.planning/phases/11-horizontal-b-spine-layout/11-02-SUMMARY.md

<interfaces>
<!-- Critical constraints from RESEARCH.md — must follow exactly -->

compute_dims memo key — INLINE TUPLE REQUIRED (named variable breaks AST test):
  CORRECT:
    if (id(node), scheme_multiplier, node_h_scale, node_v_scale, layout_mode) in memo:
        return memo[(id(node), scheme_multiplier, node_h_scale, node_v_scale, layout_mode)]
  WRONG (breaks test_compute_dims_memo_key_includes_node_h_scale):
    memo_key = (id(node), scheme_multiplier, node_h_scale, node_v_scale, layout_mode)
    if memo_key in memo: return memo[memo_key]

compute_dims new signature:
  def compute_dims(node, memo, snap_threshold, node_count, node_filter=None,
                   scheme_multiplier=None, per_node_h_scale=None,
                   per_node_v_scale=None, layout_mode="vertical"):

Mode dispatch in layout_upstream():
  root_mode = node_layout_state.read_node_state(root)["mode"]
  if root_mode == "horizontal":
      place_subtree_horizontal(root, ...)
      consumer_node, consumer_slot = _find_downstream_consumer(root, current_group)
      if consumer_node is not None:
          _find_or_create_output_dot(root, consumer_node, consumer_slot, current_group)
      layout_mode_to_write = "horizontal"
  else:
      # existing vertical path
      layout_mode_to_write = "vertical"
  # write-back: stored_state["mode"] = layout_mode_to_write

Mode dispatch in layout_selected(): per-root — each root reads its own stored mode independently.

Undo wrapping pattern for new entry points:
  def layout_upstream_horizontal(scheme_multiplier=None):
      global _TOOLBAR_FOLDER_MAP
      _TOOLBAR_FOLDER_MAP = None
      _clear_color_cache()
      current_group = nuke.lastHitGroup()   # MUST be first Nuke API call
      root = nuke.selectedNode()
      nuke.Undo.name("Layout Upstream Horizontal")
      nuke.Undo.begin()
      try:
          with current_group:
              # implementation
      except Exception:
          nuke.Undo.cancel()
          raise
      else:
          nuke.Undo.end()

Menu registration (no keyboard shortcuts — locked decision):
  layout_menu.addCommand('Layout Upstream Horizontal', node_layout.layout_upstream_horizontal)
  layout_menu.addCommand('Layout Selected Horizontal', node_layout.layout_selected_horizontal)
  Placement: after Clear Layout State commands, before Shrink separator.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entry points, mode dispatch, compute_dims memo key</name>
  <files>node_layout.py</files>
  <behavior>
    TestHorizontalAST:
      - test_layout_upstream_horizontal_exists: FunctionDef named layout_upstream_horizontal in AST
      - test_layout_selected_horizontal_exists: FunctionDef named layout_selected_horizontal in AST
      - test_compute_dims_memo_key_includes_node_h_scale: 'node_h_scale' within 80 chars of 'memo[' in compute_dims source
    TestModeReplay:
      - test_layout_upstream_dispatches_to_horizontal: layout_upstream() body contains 'horizontal' and 'place_subtree_horizontal'
  </behavior>
  <action>
    1. Extend compute_dims() signature with layout_mode="vertical" parameter. Change memo key to inline 5-tuple: (id(node), scheme_multiplier, node_h_scale, node_v_scale, layout_mode). Use inline tuple directly in both the `in memo` check and `memo[...]` store. CRITICAL: do NOT use a named variable like memo_key = (...) — the AST test requires node_h_scale to appear within 80 characters of 'memo['.

    2. Add mode dispatch to layout_upstream(): at the point where place_subtree is called, first read root_mode = node_layout_state.read_node_state(root)["mode"]. If "horizontal", call place_subtree_horizontal + _find_or_create_output_dot (after _find_downstream_consumer). Set layout_mode_to_write = "horizontal". Otherwise run existing vertical path and set layout_mode_to_write = "vertical". In state write-back loop: stored_state["mode"] = layout_mode_to_write.

    3. Add per-root mode dispatch to layout_selected() — same pattern but applied per root node in the selection loop. Each root reads its own stored mode independently.

    4. Implement layout_upstream_horizontal(scheme_multiplier=None): full undo pattern (nuke.Undo.name/begin/try/except cancel/else end), nuke.lastHitGroup() first, collect subtree nodes, build per-node scheme/scale dicts, call place_subtree_horizontal, discover consumer and call _find_or_create_output_dot, write mode='horizontal' to all touched nodes, call push_nodes_to_make_room.

    5. Implement layout_selected_horizontal(scheme_multiplier=None): same pattern for multi-root selections (guard: require >= 2 selected nodes or handle single selection gracefully), per-root horizontal placement.
  </action>
  <verify>
    <automated>python3 -m unittest tests/test_horizontal_layout.py::TestHorizontalAST tests/test_horizontal_layout.py::TestModeReplay -v</automated>
  </verify>
  <done>TestHorizontalAST 3/3 and TestModeReplay 1/1 pass GREEN. All 10 horizontal tests pass GREEN total.</done>
</task>

<task type="auto">
  <name>Task 2: Menu registration</name>
  <files>menu.py</files>
  <action>
    In menu.py, add two new commands after the Clear Layout State commands and before the Shrink separator:
      layout_menu.addCommand('Layout Upstream Horizontal', node_layout.layout_upstream_horizontal)
      layout_menu.addCommand('Layout Selected Horizontal', node_layout.layout_selected_horizontal)
    No keyboard shortcuts — locked decision from CONTEXT.md.
    No scheme variants in the menu — base horizontal commands only (Claude's discretion confirmed in CONTEXT.md).
  </action>
  <verify>
    <automated>python3 -c "import ast, pathlib; src = pathlib.Path('menu.py').read_text(); assert 'Layout Upstream Horizontal' in src and 'Layout Selected Horizontal' in src; print('OK')"</automated>
  </verify>
  <done>menu.py contains both 'Layout Upstream Horizontal' and 'Layout Selected Horizontal' command registrations. Full test suite passes: python3 -m unittest discover tests/ -q with only 4 pre-existing test_scale_nodes_axis errors.</done>
</task>

</tasks>

<verification>
python3 -m unittest tests/test_horizontal_layout.py -v
Expected: 10 tests, 10 passes, 0 failures.

python3 -m unittest discover tests/ -q
Expected: all pre-existing tests pass; only 4 pre-existing test_scale_nodes_axis errors (known nuke stub issue from Phase 10, unchanged).

python3 -c "import ast, pathlib; src = pathlib.Path('menu.py').read_text(); assert 'Layout Upstream Horizontal' in src; print('OK')"
</verification>

<success_criteria>
All 10 tests in test_horizontal_layout.py pass GREEN. layout_upstream_horizontal and layout_selected_horizontal exist in node_layout.py with full undo wrapping. layout_upstream() and layout_selected() dispatch to horizontal path from stored mode. compute_dims has layout_mode parameter and 5-tuple inline memo key. menu.py registers both horizontal commands. No regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/11-horizontal-b-spine-layout/11-03-SUMMARY.md`
</output>
