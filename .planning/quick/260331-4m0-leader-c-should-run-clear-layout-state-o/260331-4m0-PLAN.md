---
phase: quick
plan: 260331-4m0
type: execute
wave: 1
depends_on: []
files_modified:
  - node_layout_leader.py
  - node_layout_overlay.py
  - tests/test_node_layout_leader.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Pressing C in leader mode calls clear_layout_state_selected(), not unfreeze_selected()"
    - "Overlay label for C reads 'Clear State' not 'Clear Freeze'"
    - "Test assertion for Key_C reflects the new clear-state purpose"
  artifacts:
    - path: "node_layout_leader.py"
      provides: "C key dispatches to clear_layout_state_selected"
      contains: "clear_layout_state_selected"
    - path: "node_layout_overlay.py"
      provides: "Updated overlay label for C key"
      contains: "Clear State"
  key_links:
    - from: "node_layout_leader.py"
      to: "node_layout.clear_layout_state_selected"
      via: "_dispatch_clear_state function"
      pattern: "node_layout\\.clear_layout_state_selected"
---

<objective>
Fix the leader key C dispatch: change it from calling `unfreeze_selected()` (clear freeze) to calling `clear_layout_state_selected()` (clear layout state on selected nodes).

Purpose: C key is currently wired to the wrong action.
Output: Corrected dispatch, overlay label, and test assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@node_layout_leader.py
@node_layout_overlay.py
@tests/test_node_layout_leader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire C key dispatch from clear-freeze to clear-layout-state</name>
  <files>node_layout_leader.py, node_layout_overlay.py, tests/test_node_layout_leader.py</files>
  <action>
In node_layout_leader.py:
1. Rename `_dispatch_clear_freeze` to `_dispatch_clear_state` and change its body to call `node_layout.clear_layout_state_selected()` instead of `node_layout.unfreeze_selected()`. Update the docstring to say "Clear layout state on selected nodes for the C key" (remove the DISP-04 / D-12 references or update them as appropriate).
2. Update the `_DISPATCH_TABLE` entry: `Qt.Key.Key_C: _dispatch_clear_state` (was `_dispatch_clear_freeze`).
3. Update the module docstring at the top (line 6) where it says "single-shot command dispatch (V, Z, F, C)" — no change needed there, but the parenthetical description of C in the docstring body should reflect "clear state" not "clear freeze" if mentioned.

In node_layout_overlay.py:
4. Change line 40 from `("C", "Clear Freeze", 2, 2)` to `("C", "Clear State", 2, 2)`.

In tests/test_node_layout_leader.py:
5. Update `test_dispatch_key_c_present` (line 161-167): change the docstring and assertion message from "clear freeze" to "clear layout state" so the test documents the correct intent.
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/test_node_layout_leader.py -x -q</automated>
  </verify>
  <done>C key dispatches to clear_layout_state_selected, overlay shows "Clear State", test passes with updated assertion message.</done>
</task>

</tasks>

<verification>
- `grep "clear_layout_state_selected" node_layout_leader.py` confirms new dispatch target
- `grep "Clear State" node_layout_overlay.py` confirms updated overlay label
- `grep -c "clear_freeze" node_layout_leader.py` returns 0 (no leftover references to old dispatch)
- All tests pass: `python -m pytest tests/ -x -q`
</verification>

<success_criteria>
Leader key C dispatches clear_layout_state_selected instead of unfreeze_selected. Overlay label updated. Tests pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-4m0-leader-c-should-run-clear-layout-state-o/260331-4m0-SUMMARY.md`
</output>
