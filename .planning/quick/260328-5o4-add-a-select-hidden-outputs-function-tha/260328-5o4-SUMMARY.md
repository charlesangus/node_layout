---
phase: quick
plan: 260328-5o4
subsystem: ui
tags: [nuke, selection, dag, util]

requires: []
provides:
  - select_hidden_outputs() function in util.py
  - "Select Hidden Outputs" menu entry under Edit > Node Layout
affects: [util, menu]

tech-stack:
  added: []
  patterns:
    - "Selection utility pattern: iterate nuke.allNodes(), check knob, add to selection without clearing existing"

key-files:
  created: []
  modified:
    - util.py
    - menu.py

key-decisions:
  - "Do not clear existing selection — add hidden output nodes on top of the current selection so original nodes remain selected"
  - "Use id()-based set for fast O(1) lookup of selected node membership"
  - "Return early with no message when nothing is selected — consistent with other util functions"

patterns-established:
  - "id()-based set for selected node lookup: {id(n) for n in nuke.selectedNodes()}"

requirements-completed: []

duration: 5min
completed: 2026-03-28
---

# Quick Task 260328-5o4: Select Hidden Outputs Summary

**select_hidden_outputs() function that finds immediately downstream nodes connected via hidden inputs (hide_input=True) and adds them to the current selection, plus menu entry in Edit > Node Layout**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-28
- **Completed:** 2026-03-28
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- Added `select_hidden_outputs()` to util.py that iterates all nodes, finds those with `hide_input=True` connected to any selected node, and adds them to the selection without clearing the original selection
- Added "Select Hidden Outputs" menu entry to Edit > Node Layout menu, consistent with the inline import pattern used throughout menu.py
- Both files parse without syntax errors; all automated verification checks pass

## Task Commits

1. **Task 1: Implement select_hidden_outputs in util.py and add menu entry** - `abc0b27` (feat)

## Files Created/Modified
- `util.py` - Added `select_hidden_outputs()` function after `select_upstream_ignoring_hidden`
- `menu.py` - Added "Select Hidden Outputs" menu command after "Select Upstream Ignoring Hidden"

## Decisions Made
- Do not clear existing selection: hidden output nodes are added to the current selection, preserving original selected nodes. This matches the plan spec and is more useful for multi-step workflows.
- Use `id()`-based set for selected node membership check (same pattern used in node_layout.py for freeze group and layout_selected node filters).
- No keyboard shortcut assigned; user can add one manually. No shortcut was specified in the plan.
- Return early silently when no nodes selected — no nuke.message call, consistent with other util functions.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Function is self-contained in util.py; available immediately on Nuke startup via menu.py
- No blockers or concerns

---
*Phase: quick*
*Completed: 2026-03-28*
