---
phase: quick
plan: 260331-4y3
type: execute
wave: 1
depends_on: []
files_modified:
  - node_layout_overlay.py
  - nuke_tests/test_overlay_structure.py
autonomous: true
must_haves:
  truths:
    - "Overlay appears at the mouse cursor position when Shift+E is pressed"
    - "Overlay is clamped so it never extends beyond screen edges"
    - "Overlay position is static after appearing (does not follow mouse)"
  artifacts:
    - path: "node_layout_overlay.py"
      provides: "Cursor-positioned show() with screen clamping"
      contains: "QCursor"
  key_links:
    - from: "node_layout_overlay.py"
      to: "QCursor.pos()"
      via: "show() reads global cursor position"
      pattern: "QCursor\\.pos\\(\\)"
---

<objective>
Position the leader key overlay at the mouse cursor when shown, clamped to screen bounds.

Purpose: The overlay currently centers over the DAG panel, which can be far from the user's focus. Snapping to the cursor position makes it contextually relevant.
Output: Updated show() method in node_layout_overlay.py that reads QCursor.pos() and clamps to available screen geometry.
</objective>

<context>
@node_layout_overlay.py
@node_layout_leader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Position overlay at cursor with screen clamping</name>
  <files>node_layout_overlay.py</files>
  <action>
Modify the `show()` method in `LeaderKeyOverlay` to position the overlay at the mouse cursor instead of centering on the parent widget:

1. Add `QCursor` to the PySide6.QtGui import line (alongside QColor, QFont, QPainter).
2. Add `QGuiApplication` to the PySide6.QtWidgets import line (or from PySide6.QtGui — check which module it lives in; it is in PySide6.QtGui).
3. Replace the body of `show()` with:
   - Call `super().show()` first (keep existing Pitfall 4 guard — native window must exist before move()).
   - Read `cursor_pos = QCursor.pos()` — this is the global screen position of the mouse at the moment Shift+E was pressed.
   - Get the screen containing the cursor: `screen = QGuiApplication.screenAt(cursor_pos)`. If None, fall back to `QGuiApplication.primaryScreen()`.
   - Get `screen_geometry = screen.availableGeometry()` (excludes taskbar/dock).
   - Compute the top-left of the overlay such that the overlay is roughly centered on the cursor: `x = cursor_pos.x() - self.width() // 2`, `y = cursor_pos.y() - self.height() // 2`.
   - Clamp x to `[screen_geometry.left(), screen_geometry.right() - self.width()]`.
   - Clamp y to `[screen_geometry.top(), screen_geometry.bottom() - self.height()]`.
   - Call `self.move(x, y)`.
4. Remove the parent_widget centering logic entirely (the `parent_widget = self.parentWidget()` block).
5. Update the docstring to describe cursor positioning with screen clamping instead of parent centering.

Keep the method simple — no mouse tracking, no follow behavior. The position is set once on show() and stays fixed.
  </action>
  <verify>
    <automated>cd /workspace && python -c "
import ast, sys
tree = ast.parse(open('node_layout_overlay.py').read())
src = open('node_layout_overlay.py').read()
# Verify QCursor is imported
assert 'QCursor' in src, 'QCursor not imported'
# Verify QGuiApplication is imported
assert 'QGuiApplication' in src, 'QGuiApplication not imported'
# Verify old centering logic is gone
assert 'mapToGlobal' not in src, 'Old parent centering logic still present'
# Verify cursor positioning
assert 'QCursor.pos()' in src, 'QCursor.pos() not found in show()'
# Verify screen clamping
assert 'availableGeometry' in src, 'Screen clamping not implemented'
print('All checks passed')
"</automated>
  </verify>
  <done>show() positions overlay at mouse cursor, clamped to screen bounds. Old parent-centering logic removed. QCursor and QGuiApplication imported.</done>
</task>

<task type="auto">
  <name>Task 2: Update AST structure test if it validates show() internals</name>
  <files>nuke_tests/test_overlay_structure.py</files>
  <action>
Check if nuke_tests/test_overlay_structure.py has any assertions about the show() method contents (e.g., checking for mapToGlobal, parentWidget, or specific method calls inside show()). If it does, update those assertions to match the new implementation (QCursor.pos, availableGeometry, screenAt).

If the test file does not reference show() internals at all, no changes are needed — skip this task.
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest nuke_tests/test_overlay_structure.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All overlay structure tests pass with the new cursor-positioning show() method.</done>
</task>

</tasks>

<verification>
1. AST check confirms QCursor.pos() and availableGeometry in show()
2. Existing overlay structure tests pass
3. No references to old parent-centering logic remain
</verification>

<success_criteria>
- LeaderKeyOverlay.show() reads QCursor.pos() and positions overlay at cursor
- Overlay is clamped to screen.availableGeometry() bounds
- No parent-centering logic remains in show()
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260331-4y3-the-leader-popup-should-appear-at-the-mo/260331-4y3-SUMMARY.md`
</output>
