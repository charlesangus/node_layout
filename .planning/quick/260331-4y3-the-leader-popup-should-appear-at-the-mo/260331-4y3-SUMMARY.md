---
quick_task: 260331-4y3
title: "Position leader overlay at mouse cursor with screen clamping"
completed: "2026-03-31T10:36:06Z"
duration: "~5min"
tasks_completed: 2
tasks_total: 2
files_modified:
  - node_layout_overlay.py
files_created: []
key_decisions:
  - "QGuiApplication imported from PySide6.QtGui (not QtWidgets) — correct module location"
  - "show() position set once on call — no mouse tracking, fully static after appear"
  - "Task 2 was a no-op: nuke_tests/test_overlay_structure.py does not exist"
commits:
  - hash: bcf531a
    message: "feat(quick-260331-4y3): position overlay at cursor with screen clamping"
tags: [overlay, ui, cursor-positioning, screen-clamping]
---

# Quick Task 260331-4y3: Position Leader Overlay at Mouse Cursor

**One-liner:** Replaced parent-widget centering with QCursor.pos()-based positioning and screen.availableGeometry() clamping in LeaderKeyOverlay.show().

## What Was Done

### Task 1: Position overlay at cursor with screen clamping

Modified `show()` in `LeaderKeyOverlay` (node_layout_overlay.py):

- Added `QCursor` and `QGuiApplication` to the `PySide6.QtGui` import line.
- Replaced the `parentWidget()`/`mapToGlobal()` centering block entirely.
- New logic: reads `QCursor.pos()` at call time, gets the screen containing the cursor via `QGuiApplication.screenAt()` (falls back to `primaryScreen()` if None), computes centered position, then clamps both x and y to `screen.availableGeometry()` bounds.
- Position is set once via `self.move(x, y)` — no follow/tracking behavior.

### Task 2: Update AST structure test (no-op)

`nuke_tests/test_overlay_structure.py` does not exist in the repository. No test file contains assertions about `show()` internals, so no changes were needed.

## Verification

All plan-specified checks passed:
- `QCursor` in source: YES
- `QGuiApplication` in source: YES
- `mapToGlobal` absent: YES (old centering logic fully removed)
- `QCursor.pos()` in source: YES
- `availableGeometry` in source: YES

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check

- [x] `node_layout_overlay.py` exists and contains cursor-positioning logic
- [x] Commit `bcf531a` exists in git log
- [x] No `mapToGlobal` or `parentWidget` references remain in `show()`
